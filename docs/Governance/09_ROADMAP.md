# ROADMAP — Nexus by MSI

> Status fitur & modul. Sumber: `CLAUDE.md` (roadmap fase 2.0A–2.10F) + `PROGRESS.md` + `AGENTS.md` (arah produk). Detail per-fase granular ada di **git history `CLAUDE.md`** + `PROGRESS.md`.
>
> **Legenda:** ✅ Done · 🔄 In Progress · 📋 Planned · ⏸ Deferred
>
> **Diperbarui 2026-07-25 — dedup akun: HARD-BLOCK per-entitas AKTIF + lapisan UX di atasnya** (3 file FE; **NOL perubahan DB dari sesi itu** — index/fungsi dipasang manual sebelumnya). **⭐ Keputusan Terbuka #20 / "K-3" DITUTUP** — aturan *"satu perusahaan = satu akun, PER-ENTITAS, hard-block"* kini ditegakkan **di level tabel** lewat UNIQUE index partial `uq_accounts_norm_name_per_entitas` (+ fungsi `normalize_account_name`, + `pg_trgm`); prasyarat merge 7 grup duplikat **sudah dijalankan**. Lapisan UX-nya: pre-check **fuzzy non-blocking** (RPC `check_similar_accounts`, SECURITY DEFINER supaya tak buta terhadap akun sales lain) + **handler `23505` manusiawi** di **tiga** jalur create. **✅ [SUSULAN 25 Jul — utang JEJAK LUNAS]** keempat objek dedup kini direkam di migrasi `20260725000001`–`000004`; `check_similar_accounts` per Den sudah dijalankan → **TD-128 RESOLVED (jejak)**. **⚠️ Sisa 2 utang MANUAL:** snapshot belum di-`pg_dump` (masih **menyesatkan** untuk `accounts`; **"berjejak migrasi" ≠ "ada di snapshot"**) + drop 6 tabel backup lama (18–23 Jul). ~~Lapisan fuzzy sendiri **belum tes runtime** (RPC ada, form di balik login). **NOL tes runtime**~~ **[KOREKSI SUSULAN 25 Jul 2026 — TESTED]** Lapisan fuzzy **sudah tes runtime**: RPC dites langsung via `SELECT` (`check_similar_accounts('ALLIANCE COSMETIC', <uuid MSI>)` → 3 baris, similarity 1.0/0.842/0.619) dan smoke test UI membuktikan warning *"Mirip dengan: ALLIANCE COSMETICS"* **muncul** di jalur Prospect **dan** Customer CRM — TERBUKTI JALAN, bukan lagi hipotesis. Sisi lain batch (handler `23505`, pelebaran cakupan dup-check) belum ada laporan tes runtime terpisah. **lint net-zero terverifikasi.** Sebelumnya **2026-07-24 — fix aging Lead Pool + pembersihan data DUA GELOMBANG (27 + 15 akun)** (1 Edge Function `aging-pipeline` + **DUA** migrasi **data**; **NOL file FE, NOL perubahan RLS/policy, NOL objek DB baru**). Tiga perbaikan: **(1)** reset kelima kolom `pull_*` saat mem-pool (**AKAR** — cap `'approved'` basi dari siklus lama mengunci akun karena Lead Pool & Approval membaca sumbu berbeda); **(2)** `PROPOSAL`/`NEGOTIATION` dicabut dari aturan aging (dipensiun 22 Jul → `stage_changed_at` beku → akun warisan ke-pool berulang tanpa jalan keluar; PROPOSAL justru berambang tersempit 3 hari); **(3)** `updated_at` inquiry & quotation jadi **sinyal aktivitas keempat** (aging sebelumnya buta terhadap dokumen). **✅ Terverifikasi:** `deno check` exit 0 (baseline juga 0) + `dry_run` + **1 run live → 6 akun ter-pool, semuanya `pull_status=null`, semuanya CONTACTED** *(pengamatan Den)*. **⚠️ Belum tes runtime UI.** **Pembersihan data DUA GELOMBANG:** **(a) 27 akun korban trap** (`20260724000000`, **idempotent** — `pull_*` ikut di-reset); **(b) 15 akun opsi C1** (`20260724000001`, **TIDAK idempotent**) = ≥2 quotation hidup (14) + 1 anomali customer WON — di gelombang ini **`pull_*` TIDAK disentuh** (sudah `null`) dan `pull_status='pending'` **dikecualikan**. **~39 akun berdokumen tipis SENGAJA dibiarkan** di pool: `pull_status` null → tombol "Tarik ke Pipeline" muncul → **sales menariknya sendiri** lewat approval; **bukan sisa pekerjaan**. Keduanya tak menyentuh `pipeline_stage`/`account_status`. +**TD-124/125/126** (LOW) +**TD-127 (MEDIUM** — customer WON nyangkut di pool; **permukaan BEDA** dari fix EF: EF mengurus *siapa yang MASUK* pool, TD-127 *akun yang status-nya BERUBAH sesudah di pool*; baris dibersihkan, **akar belum ditutup**) · **+Keputusan #20** ("satu perusahaan = satu akun", per-entitas, hard-block — **sudah diputuskan, BELUM dieksekusi**). ~~**Snapshot SEGAR, nol utang `pg_dump`**~~ **[KOREKSI — hanya berlaku untuk gelombang 1]** ⚠️ **ADA utang `pg_dump`** (kecil): `backup_leadpool_c1_won_20260724` dari gelombang 2 **belum masuk snapshot** (dump terakhir jalan **di antara** dua gelombang; diverifikasi grep → 0 hit). **Tabel backup kini 8** — drop sekaligus lalu **SATU** dump (lihat `03_DATA_MODEL` gotcha #10). Sebelumnya **2026-07-22 — CRM Fase 3 batch 3B-1 (sisi TULIS): frontend berhenti menulis nilai stage lama** (FE-only 9 file, **NOL perubahan DB/RLS/migrasi**). Sumbu deal sudah pindah ke `inquiries.status` di **Fase 2** (tiga trigger DB); batch ini menutup **seluruh** pintu tulis `accounts.pipeline_stage` selain **NEW/CONTACTED/QUALIFIED**, **tanpa** menyembunyikan data lama (**daftar RENDER tetap 7 nilai**). +aksi **"Tandai Kalah"** per-inquiry (menulis `inquiries.status='LOST'` + `lost_reason`, alasan wajib). **Pemangkasan kolom + CHECK = batch 3C** (setelah backfill) — **belum dikerjakan**. **Susulan hari yang sama:** "Tandai Kalah" kini **berjejak di `audit_logs`**; **lima SQL manual Fase 3 akhirnya DIREKAM** sebagai migrasi `20260722000006`–`20260722000010` (backup accounts · tiga kolom deal di `inquiries` · trigger konversi baru **`set_customer_on_inquiry_won`** · **revisi pemicu WON → SO berstatus `SENT`** · pembersihan data uji SO/inquiry/counter — **satu-satunya yang mengubah data produksi**). **Ketiga Keputusan Terbuka #17/#18/#19 SUDAH DIJAWAB** (lihat di bawah). **✅ TES RUNTIME: LOLOS SEBAGIAN** *(Vercel preview, akun **sales ASLI (Karina), bukan super admin** → jalur RLS benar-benar teruji; **pengamatan Den**, bukan verifikasi doc-keeper)* — **terbukti:** papan Kanban (7 kolom tampil, kartu PROPOSAL/NEGOTIATION terlihat; drag ke PROPOSAL **dan** WON ditolak toast + WinLossModal tak muncul + nol tulis DB; NEW→CONTACTED normal) · Detail Account (Pindah Stage 3 opsi; Edit Deal 7 opsi 4 disabled & PROPOSAL tetap terbaca; **simpan nilai pada akun PROPOSAL → `stage_changed_at` TIDAK bergerak** = bukti independen stage tak ikut ditulis) · gate BANT lintas-jalur (pesan identik Kanban↔Detail Account) · Tandai Kalah (tanpa kategori ditolak; dengan kategori → `status='LOST'`+`lost_reason`, **akun pemilik nol perubahan**, `audit_logs` tercatat). **BELUM teruji:** `ProspectFormPage` · jalur **stage** `DealDetailPage` · jalur Lead Pool (clamp `prevActiveStage`) · funnel CRM Dashboard. ~~⚠️ **SNAPSHOT BASI — utang tinggal SATU: `pg_dump`**~~ **[KOREKSI 22 Jul 2026 — USANG] `pg_dump` SUDAH DIJALANKAN sesudah seluruh SQL manual Fase 3 → `schema_snapshot.sql` SEGAR** (terverifikasi memuat 3 kolom deal `inquiries`, fungsi+trigger `set_customer_on_inquiry_won`, `trg_inquiry_won` versi `AFTER INSERT OR UPDATE` `:7829`, dua tabel backup). **Tidak ada utang `pg_dump` untuk Fase 3.** Aturan umum tetap berlaku: *"snapshot bilang objek tidak ada ≠ objeknya tidak ada"* — bandingkan tanggal refresh vs tanggal SQL manual terakhir, lalu TANYA. ⚠️ Sebaran status inquiry dari Fase 2 (WON 3/QUOTED 66/OPEN 136) **sudah tidak berlaku** pasca-pembersihan data uji — jangan dikutip sbg kondisi hari ini. Sebelumnya **2026-07-17 — Quotation: TABEL KURS manual per-quotation** (kolom baru `quotations.exchange_rates` jsonb + RPC `save_quotation` memetakannya — **DB sudah LIVE**, 2 file FE; **tes runtime OK**; migrasi direkam (`20260717000000`+`20260717000001`) + snapshot ter-refresh → **TD-74 (a)+(b) beres**, sisa **(c)** tabrakan nama + **(d)** `usd_rate` = keputusan desain). ⚠️ Arah *"dropdown currency header tunggal jadi penggerak baris"* yang sempat dicatat di sini **DIBATALKAN & dibongkar** — lihat baris Quotation di Status Modul + `PROGRESS.md` 2026-07-17. Sebelumnya 2026-07-10 — Modul PRF (Procurement) Fase 1+2 LIVE (form Section 01/02/03/04 + child fields dinamis + menu + nomor auto); FASE 0-3 SP done (mesin status LIVE s/d BTB_TERBIT), FASE 4-5 + tech debt next. Fakta: `03_DATA_MODEL`/`05_WORKFLOW_MAP`/`08_TECH_DEBT`.
>
> ℹ️ **Audit sumber 13-18** (`13_CRM_FLOW_AUDIT`, `14_BACKLOG_RECON`, `15_INPUT_CONTROL_AUDIT`, `16_SP_TABLES_SYNC_AUDIT`, `17_ZERO_INPUT_AUDIT`, `18_CRM_SALES_PRF_PENDING_AUDIT`) **kini diarsipkan di `docs/archive/audits/`**. Tag `(dari NN_…)` di bawah = atribusi historis (bukan tautan hidup).

---

## Status Modul

| Modul | Sub-fitur | Status | Notes |
|-------|-----------|--------|-------|
| **Auth & Access** | Supabase Auth + RLS | ✅ | Company- + role-scoped |
| | RBAC (modules→menus→actions, `user_menu_permissions`) | ✅ | **[7 Agu 2026]** `hasPermission()`/`role_permissions` (sistem lama) **pensiun dari kode** (tab Roles dihapus, dead branch `App.jsx` dihapus, fetch dimatikan `AuthContext.jsx` — objek DB dibiarkan ada, sengaja tak dipakai; TD-06 kini PARTIAL). `hasMenuPermission` kini resolusi **3-tingkat**: super_admin > user override (`user_menu_permissions.effect` grant/deny, tabel `role_menu_permissions` baru = role-level default) > deny. RLS tetap belum sinkron (TD-01) — axis itu TAK berubah. **[9 Agu 2026]** +2 menu masuk cakupan granular: `logistics_picking`/`logistics_surat_jalan` (Picking List & Surat Jalan, dulu murni `role[]` hardcode, migrasi `20260809000001`) — lihat `04_ROLE_PERMISSION_MATRIX.md` §Peta Menu. `hasMenuPermission`/`pickPrimaryErpRole` juga kini **company-aware** (scoped ke `activeCompanyId`, lihat baris "Multi-entity switcher" di bawah) — no-op perilaku hari ini krn belum ada UI yg mengubah `activeCompanyId` dari default home company. Lihat baris "Role Defaults" di bawah + `03_DATA_MODEL.md` §3. **[20 Agu 2026]** Sistem lama itu kini **dihapus fisik**, bukan lagi "pensiun tapi strukturnya dibiarkan": `hasPermission`/`isCrossEntity`/`userPermissions`/`refreshPermissions`/`fetchPermissionsForRoleId` hilang dari `AuthContext.jsx`, dan parameter `hasPermission` dicabut dari 5 fungsi gate + props `NexusSidebar` di `App.jsx` (tanda tangan kini seragam `(item, role, hasMenuPermission, isBnfAuthorized)`). Tabel `role_permissions` karenanya **nol pembaca** — penulis tersisa cuma halaman orphan `RolesPage.jsx` (`08_TECH_DEBT.md` **TD-204**). ⚠️ **Bukan berarti permission FE sudah beres**: sistem KETIGA `can()`/`PERMISSIONS` (TD-182) masih menggerbang 10 titik Finance/Master Customer, tak tersentuh. Terpisah, sesi sama: 2 array `role:[]` mati (`picking`/`surat-jalan`) dihapus — sisa kerja 9 Agu, **nol perubahan perilaku** (gate efektifnya sudah `hasMenuPermission` sejak saat itu). |
| | **Role Defaults** (default izin menu per role) | ✅ **[BARU, 7 Agu 2026]** | Halaman `RoleDefaultsPage` (kini di dalam **AdminHub**, super_admin only, gate di titik render bukan cuma sidebar) — atur `role_menu_permissions` per role + fitur "Salin dari role lain" (destructive replace, dialog konfirmasi wajib). ~~**⚠️ Tabel `role_menu_permissions` KOSONG TOTAL saat ini**~~ **[KOREKSI 9 Agu 2026]** tidak lagi 100% kosong — migrasi `20260809000001` mengisi baris utk Picking List/Surat Jalan × 5 role (scoped-fix, BUKAN Fase B3). **Fase B3 (isi default komprehensif SEMUA menu × role) tetap belum dimulai** — lihat Next Up. Fase C (satukan menu hardcode `App.jsx` dgn katalog DB) juga belum dimulai. |
| | User Access (list, edit page, avatar upload, permission matrix) | ✅ | EF delete-user/reset-password dibuat, **deploy pending** (TD-21). **[9 Agu 2026]** Kini di dalam **AdminHub** (bukan lagi `AdminShell`); redirect legacy `'users'` landing TEPAT di card User Access (dulu landing di default hub). Tab "Permissions" `UserEditPage` digate `isSuperAdmin` (dulu tampil+bisa diklik utk `admin` biasa, padahal RLS `user_menu_permissions` cuma izinkan tulis `super_admin` — trap-UX, sekarang ditutup). |
| | **Multi-entity switcher — Fase 1 plumbing** [BARU, 9 Agu 2026] | ✅ (plumbing) | `AuthContext.jsx` expose `activeCompanyId`/`setActiveCompanyId` (session-only, default home company)/`myCompanyIds` (semua company user py role aktif); `pickPrimaryErpRole` & tier-3 `hasMenuPermission` kini scoped ke company aktif. UI switcher pertama: `src/components/CompanySwitcher.jsx` (topbar, ganti tombol dekoratif mati "MSI/JCI/Storbit" — **TD-92 RESOLVED**) — label statis (1 company) atau pill-switcher (>1, filter `myCompanyIds`). **Diuji dgn user multi-company asli** (Elvira Nurhuda, Finance MSI+SOA) — sempat menyingkap RLS `companies_read_own` yang memotong hasil ke 1 company saja; sudah ditambal (di luar sesi kode ini). Pola RLS yang sama ditemukan berulang di `roles_read` (ditambal) & `payment_terms_read` (**belum**) — `08_TECH_DEBT.md` **TD-180** (OPEN, audit lanjutan dibutuhkan). Detail: `03_DATA_MODEL.md` §RBAC. |
| | Auth lifecycle hardening (logout cleanup, restored-menu guard, content gate, tab-switch state) | ✅ | Fase 2.3D–F, 2.8B |
| | **Notification bell** (badge, dropdown, mark-as-read) | ✅ | 4 producers: activity assign/done, HRGA submit/approve; TDZ-safe (2.10K); **belum tes manual runtime** |
| | **Pending Approval badge** (HRGA pending count) | ✅ | auto-refresh 60s, navigasi ke inbox HRGA (2.10J); **belum tes manual runtime** |
| | `profiles.role` deprecation | 🔄 | Tahap 1–3 selesai; drop kolom = tahap 4 pending (TD-20) |
| **Foundation / Master Data** | Companies, Branches, Departments, Roles, Products, Document Types, Payment Terms, Taxes, Status Catalog, **DC Master** [BARU, 8 Agu 2026] | ✅ | CRUD live. `dc_master` (`DcMasterPage.jsx`) dulu read-only (dropdown SP saja), kini CRUD penuh — lihat `03_DATA_MODEL.md`. **[9 Agu 2026]** Semua kini di dalam **AdminHub** (`admin-hub`, ganti `AdminShell` — lihat baris "AdminHub" di bawah), bukan lagi entry point tersendiri. **[13 Agu 2026]** 9 halaman dimigrasi ke kit desain resmi `admin-settings/kit.jsx`+`tokens.js` (lepas dari `PASTEL` lokal + `AdminPageHeader`), 3 batch hari yang sama: Batch 1 read-only (Companies/Document Types/Status Catalog/Taxes/Payment Terms), Batch 2 CRUD (Branches/Departments/DC Master), Batch 3 kasus khusus multi-entitas (Positions — array `ENTITIES` lokal dipertahankan, kit `EntitySwitcher` dievaluasi & tak dipakai krn single-select). Sisanya (Roles/Role Defaults/User Access) masih pola lama. Presentasi murni, nol perubahan CRUD/RLS. Detail: `08_TECH_DEBT.md` TD-14 (PARTIAL, 9/22 file) · `PROGRESS.md` 2026-08-13. |
| | Positions (compact group-by-code + checkbox entitas) | ✅ | 2.9T |
| | Struktur Organisasi (Org Chart, `reports_to`, warna per-level) | ✅ | 2.9S/U/V |
| | Schema Manager | ✅ | via EF `manage-schema`. **[9 Agu 2026]** Card di dalam AdminHub, bukan lagi leaf top-level tersendiri — blok render `App.jsx` lama masih ada (unreachable dari sidebar normal, `08_TECH_DEBT.md` TD-181). |
| | **AdminHub** — unifikasi Master Data + Admin Settings [BARU, 9 Agu 2026] | ✅ | `src/pages/foundation/AdminHub.jsx` (landing card-grid, 25 destinasi, 6 grup) menggantikan `AdminShell.jsx`+`AdminSettingsHub.jsx`(+9 sub-halaman) sbg SATU entry point. Gating 2-lapis (visibility card + render-guard tunggal) mereplikasi PERSIS kondisi akses asli tiap halaman — nol pelonggaran/pengetatan disengaja, kecuali 1 asimetri pre-existing yang diwarisi apa adanya (Schema Manager, lihat `04_ROLE_PERMISSION_MATRIX.md`). File lama pensiun dari `App.jsx` tapi dibiarkan di disk (orphan, `08_TECH_DEBT.md` **TD-181**, sama pola `AppLauncher.jsx`). Detail: `04_ROLE_PERMISSION_MATRIX.md` §FOUNDATION. |
| | Admin Settings (Entity profile/bank/signatories, Document numbering/templates, Finance defaults, Approval workflows, Notifications) | ✅ | UI + Supabase wired (2.6A–F). **[9 Agu 2026]** Finance defaults — Termin Pembayaran Default berubah dari `NumberStepper` manual jadi dropdown `payment_terms` aktif per entitas (kolom baru `entity_finance_settings.default_payment_term_id`, FK) — lihat `03_DATA_MODEL.md` §Foundation. Kini diakses via AdminHub (baris di atas), bukan `AdminSettingsHub` langsung. |
| | **Admin Settings — Security Policy, Audit Log, General Preferences, Integrations** | ✅ | port Lovable (2.11A); General/Security/Integrations = localStorage fallback (TD-36), Audit Log = fetch real **`audit_logs`** (`AuditLogPage.jsx:103`) — sejak 2.11J (`7e40149`, 24 Jun); **TD-37 DONE** (klaim login-only usang). Sisa opsional: diff viewer `old_data`/`new_data`; **belum tes manual runtime**. **[9 Agu 2026]** Kini diakses via AdminHub, bukan `AdminSettingsHub` langsung. |
| | **AdminKit (`kit.jsx`) extended** | ✅ | +13 ikon lucide + primitive `KitSelect` (2.11A); satu sumber design-system Admin Settings |
| | **Dropdown Management** (master dropdown/option values) | ✅ | full DB-driven via `dropdown_options` (CRUD persist) + `currencies`/`payment_terms` (toggle-only); Tab 2 di General Preferences (2.11C–E); **belum tes manual runtime** |
| | My Profile (overlay, avatar, password, prefs) | ✅ | 2.8A |
| **CRM & Inquiry** | Pipeline / Kanban (drag-stage, soft-gate, toolbar: member/sort/filter/list-view) | ✅ | 2.9X–Z; `estimated_value`. **[22 Jul 2026 — batch 3B-1]** **7 kolom tetap dirender**, tapi **drop target tinggal NEW/CONTACTED/QUALIFIED** (target lain ditolak toast, nol tulis DB); mesin tulis WON/LOST + gate handover **dicabut** dari papan akun (sumbu deal → `inquiries.status`). Gate BANT kini berlaku di **semua** jalur tulis via helper bersama `bantQualifyGate()`. **✅ TES RUNTIME LOLOS** (sales asli, bukan super admin): 7 kolom tampil + kartu PROPOSAL/NEGOTIATION terlihat · drag ke PROPOSAL **dan** WON ditolak toast, **WinLossModal tak muncul**, nol tulis DB · NEW→CONTACTED normal · gate BANT 0/12 ditolak dgn pesan identik di Kanban & Detail Account. **Belum teruji: funnel CRM Dashboard** yang kini dirender dari daftar stage turunan |
| | **Sumbu deal per-inquiry** (`inquiries.status` + 3 trigger DB) | ✅ LIVE | Fase 2, migrasi `20260722000004`: PRF SUBMITTED→`IN_REVIEW` · quotation SENT→`QUOTED` · **`trg_inquiry_won` DIREVISI `20260722000009`: pemicunya SO berstatus `SENT`** (bukan "SO dibuat"), event `AFTER INSERT OR UPDATE`. "Hanya naik" → `LOST`/`CANCELLED` manual; **`NEGOTIATION` belum punya penulis otomatis**. ✅ versi baru sudah termuat snapshot (`:7829`) |
| | **Konversi akun→customer dari inquiry WON** (`trg_set_customer_on_inquiry_won`) | ✅ LIVE | `20260722000008` (F3-3) — **pemicu konversi PINDAH** dari `accounts.pipeline_stage='WON'` ke inquiry yang menang; trigger lama `set_customer_on_won` **sengaja masih hidup berdampingan** sampai **F3-8 (batch 3C)** |
| | **Tiga kolom deal di `inquiries`** (`won_reason`/`lost_reason`/`estimated_value`) | ✅ LIVE | `20260722000007` (F3-2) — **nullable TANPA default, sengaja beda dari `accounts.estimated_value` yang DEFAULT 0** supaya "belum diisi" ≠ "nol". Kolom lama di `accounts` belum di-drop/di-backfill (3C) |
| | **"Tandai Kalah" per-inquiry** (`status='LOST'` + `lost_reason` wajib) | ✅ | 22 Jul, `DealDetailPage`; allowlist status belum-terminal (fail-closed); `accounts` nol sentuhan; izin dari **RLS `inquiries_update`** (sales hanya inquiry buatannya); **berjejak di `audit_logs`** (`UPDATE_INQUIRY`, notes memuat status lama + alasan). **✅ TES RUNTIME LOLOS end-to-end** (sales asli): tanpa kategori **ditolak** · dengan kategori → `status='LOST'` + `lost_reason` terisi · **akun pemiliknya NOL perubahan** (tetap `prospect`/PROPOSAL) → aturan "inquiry kalah tak menurunkan lifecycle akun" **terbukti di data** · `audit_logs` memuat `UPDATE_INQUIRY` notes `"OPEN → LOST · alasan: …"`. Data uji sudah dikembalikan ke `OPEN` |
| | **Pemangkasan `accounts.pipeline_stage` jadi 3 nilai + CHECK** | 📋 batch 3C | Prasyarat: backfill baris warisan (PROPOSAL/NEGOTIATION/WON/LOST/NURTURE) + keputusan nasib NURTURE (TD-61) & konversi WON→customer (TD-94 / Keputusan Terbuka #18) |
| | Prospect form + BANT scorecard + Win/Loss capture | ✅ | auto-assign sales, dup-check |
| | Inquiry (list + form + detail) | ✅ | **[23 Jul 2026]** InquiryListPage +**chip filter status** (per nilai `inquiries.status` yang punya data, angka = baris saat diklik) + kolom **"Umur Inquiry"** (hari sejak `created_at`) = **jembatan visibilitas sumbu deal sampai papan Deal F4-6**; FE-only, ⚠️ belum tes runtime. **[KOREKSI 25 Jul 2026 — baris ini hanya mencatat kerja PAGI 23 Jul]** commit `72688ec` (chip filter, pagi) BUKAN satu-satunya kerja hari itu — **SORE hari yang sama** (`260e9ae`, 14:46) `InquiryFormPage` digarap ulang: field "Commodity" dihapus total, `goods_name` ("Nama Barang") jadi **satu-satunya** field nama barang (3 pembaca lain — `CustomerDetailPage`/`DealDetailPage`/`InquiryPDF` — ikut disesuaikan, baris "Komoditas" duplikat dihapus). **25 Jul 2026 (susulan, B4):** kolom `inquiries.commodity` **SUDAH DI-DROP** dari DB (backfill 80 baris dulu, lalu `ALTER TABLE … DROP COLUMN`, migrasi `20260725000006`) — bukan lagi "belum", kolomnya sungguh tak ada lagi. Detail: `PROGRESS.md` 2026-07-25 (entri PRF batch B1 + Inquiry B3 + drop kolom B4) |
| | Quotation (builder, SLA BD-05, pricing authority BD-06, discount, currency dropdown, VAT rate per service, PDF) | ✅ | PDF = `@react-pdf/renderer` (2.10A–C); **currency EUR/SGD/JPY/MYR + VAT rate dropdown** ✅ (2.10C/H/I). **"currency dropdown" di baris ini = dropdown per-baris** (`quotation_items.currency`) — **currency tetap per-baris (multi-currency)**; ⚠️ tak ada dropdown currency di header (percobaan 17 Jul **dibatalkan**, `quotations.currency_code` tetap PASIF). ⚠️ **"pricing authority BD-06" di baris ini = UI SAJA** (badge otoritas diskon + tampilan margin) — **enforcement NOL** (terverifikasi 17 Jul): `validate()` (`QuotationFormPage.jsx:784-788`) HANYA cek `header.inquiry_id`; `handleSave` (`:799`) = `if (!validate()) return false;` titik. `pricingAuthority()` (`:41`) dipanggil sekali di `:1118` → cuma merender badge warna; `marginPct` (`:780`) cuma dirender (`:1290`); `margin_floor` cuma passthrough payload (`:869`) — **tanpa field input, tak pernah dibandingkan**. Artinya user bisa **submit diskon 90% / margin negatif**: badge merah "perlu approval CEO" muncul tapi **simpan tetap jalan**, dan **tak ada record approval ditulis ke mana pun**. Lihat **TD-38** (HIGH) + task **H3** (`10_TASK_BREAKDOWN.md`). Sumber: audit 17 Jul (`18_CRM_SALES_PRF_PENDING_AUDIT.md` GAP #3 / C-03) |
| | **Tabel kurs per-quotation** (`quotations.exchange_rates` jsonb) | ✅ | 17 Jul: header punya tabel kurs manual; **kurs baris jadi read-only turunan** + write-through ⇒ Detail/PDF nol perubahan; seed quotation lama (beda kurs antar-baris → warning eksplisit); blokir **SUBMIT saja** bila kurs kurang (Draft boleh). **DB LIVE** (ALTER + RPC, manual) + **migrasi direkam** (`20260717000000`+`20260717000001`) + **snapshot ter-refresh**; **tes runtime OK**. Sisa **TD-74 (c)** tabrakan nama & **(d)** `usd_rate` = keputusan desain, bukan fitur kurang. Kurs **manual per-quotation, tanpa lookup FX** → **B6** `14_BACKLOG_RECON` tetap **SEBAGIAN** (jangan diklaim tutup) |
| | CRM Dashboard (KPI, charts, calendar, per-role, activity report) | ✅ | bg putih 2.10E |
| | **CRM Report page** (KPI, trend chart, per-sales breakdown, activity detail) | ✅ | Supabase real data + sidebar menu Report (2.10L–M); **belum tes manual runtime** |
| | Master Customer (list + detail page + health score) | ✅ | per-entitas + Free Agent (2.1C–G). **[26 Jul 2026]** +tab **"Kontak"** (`CustomerDetailPage`) — CRUD kontak/PIC per akun (tabel baru `contacts`, dibuat manual Den; NOL gate role tambahan, warisan visibilitas `accounts` via RLS); ⚠️ belum tes runtime. **⭐ [26 Jul 2026, lanjutan hari yang sama — Fase 1 TUNTAS]** migrasi arsip `20260726000001` merekam DDL + backfill **991 kontak / 239 activities tertaut**; **10 file CRM** dipindah baca kontak primary `contacts` (bukan lagi `accounts.pic_name/pic_phone/pic_email`) — list Customer/Prospect, kartu Kanban, detail+PDF Quotation, kartu Storbit; jalur tulis `pic_*` **dikunci** read-only di 3 form. ~~**⚠️ Utang `pg_dump` MELEBAR** (kini +2 kolom FK `activities.contact_id`/`inquiries.contact_id`, belum sekadar `contacts`) — belum direkam migrasi debt itu sendiri **sudah LUNAS**, tapi `pg_dump` tetap terbuka~~ **[KOREKSI 27 Jul 2026 — doc-keeper, temuan insidental] USANG** — ketiganya sudah termuat snapshot sejak commit `37ac4fd` sendiri (26 Jul 21:31, commit yang sama yang menulis klaim ini). Detail bukti: `03_DATA_MODEL` entri `contacts` (`08_TECH_DEBT` TD-134/**TD-131**/**TD-132**/**TD-133**, `PROGRESS.md` 2026-07-26) |
| | Lead Pool | ✅ | 2.4A. **[24 Jul 2026 — fix aging]** EF `aging-pipeline` kini **me-reset kelima kolom `pull_*`** saat mem-pool (akar "akun terkunci: Lead Pool bilang Disetujui, Approval page kosong") · aturan aging tinggal **NEW 7 · CONTACTED 5 · QUALIFIED 5** (`PROPOSAL`/`NEGOTIATION` dicabut — stage-nya dipensiun 22 Jul, `stage_changed_at` beku) · sinyal "disentuh" +**sumbu dokumen** (`updated_at` inquiry & quotation). **Pembersihan DUA GELOMBANG: 27 akun korban trap** (`20260724000000`, idempotent) **+ 15 akun opsi C1** (`20260724000001`, **TIDAK idempotent**; ≥2 quotation hidup + 1 anomali customer WON; `pull_*` tak disentuh, `pending` dikecualikan). **~39 akun berdokumen tipis sengaja dibiarkan** — sales tarik sendiri lewat UI. ✅ `deno check` + `dry_run` + 1 run live (6 akun, semua `pull_status=null`, semua CONTACTED); **⚠️ belum tes runtime UI**. ⚠️ **TD-127** (MEDIUM): customer WON bisa nyangkut di pool — akar belum ditutup. Detail: `05_WORKFLOW_MAP` §Aging Pipeline |
| | Activities (unified call/visit/meeting/email/followup/wa) + Activity Log feed | ✅ | gantikan sales_calls/visits (2.9D–N) |
| **Logistics (Storbit SP)** | **Mesin status SP 12 tahap** (FASE 0-4): skema baru `sp_orders`/`sp_order_items`/`sp_btb`/`dc_master` + `sp_recompute_status` (fact-derived) | ✅ | **LIVE s/d SUBMITTED.** **[8 Agu 2026] INVOICED/SUBMITTED (FASE 4) KODE+UI+SQL SEMUA LIVE** (tabel `sp_invoices`/`sp_invoice_lines`/`sp_payments` + RPC `create_invoice`/`submit_invoice` + fix guard `sp_recompute_status` — dikonfirmasi via `schema_snapshot.sql` refresh commit `8117127`, 23:33 WIB); ~~**LUNAS (FASE 5) TETAP 📋 belum dibangun sama sekali**~~ **[17 Agu 2026 — FASE 5 LIVE, rantai 12 tahap LENGKAP]** RPC baru `record_payment`/`mark_ttf_received` + 2 tabel jurnal AR (`journal_entries`/`journal_entry_lines`) + cabang `WHEN v_paid THEN 'LUNAS'` di puncak CASE `sp_recompute_status` + UI pembayaran/TTF inline di card Invoice — dikonfirmasi live via `schema_snapshot.sql` refresh commit `35cb1d3`, **plus laporan tes end-to-end satu siklus penuh dari Den (klaim di-relay)**. Detail: `03_DATA_MODEL`/`05_WORKFLOW_MAP` + Selesai Terbaru (16-17 Agu 2026) di bawah |
| | **Dashboard Storbit** (Shipping Manifest + Warehouse) | ✅ **BARU 18 Agu 2026** | `StorbitDashboardPage.jsx` + 3 RPC agregasi/drill-down (SECURITY INVOKER + STABLE, ACL pola FASE 5 — GRANT `authenticated` saja, nol `anon`). 6 kartu manifest + 2 kartu tenggat + 1 kartu risiko pinalti (wajib berpasangan penyebut) + donut 6 slice; 3 kartu warehouse + donut kesehatan stok; baris drill-down bisa diklik ke Detail SP / Detail Produk. Menu key **reuse `logistics_sp`** (bukan key baru — `hasMenuPermission` default-deny). **Cakupan hari ini:** kotak "Cari nomor SP" masih kosmetik (`disabled`); `terkirim_penuh` & `pernah_risiko_pinalti` belum punya pintu masuk drill-down di UI; `products.reorder_point` belum diisi sama sekali → kartu "Danger Stock" selalu 0 dan "ROP Belum Diisi" = seluruh produk aktif. |
| | Input SP single-door + penomoran manual + DC wajib + identitas komposit `(customer_id,sp_no)` + dual-write | ✅ | InputSPPage (FASE 0) |
| | Picking → Surat Jalan → Dispatch (isi `shipped_qty`) → BTB (`sp_issue_btb`, Detail SP) | ✅ | FASE 1-3; picking/delivery RPC. **[21 Agu 2026] Partial picking — kode FE selesai, ⚠️ SQL guard BELUM DIJALANKAN, NOL tes runtime **[KOREKSI 24 Agu 2026, doc-keeper: SUDAH DIJALANKAN — header migrasi `Status: LIVE`, guard baru terbaca di `schema_snapshot.sql:795`; hanya "NOL tes runtime" yang masih berlaku]**:** input qty per item (ganti toggle biner) + status item `pending`/`short`/`picked` diturunkan dari angka + "Selesaikan Picking" tak lagi bersyarat (dialog konfirmasi kalau belum lengkap; **ditolak** kalau seluruh baris masih 0 — gate `allItemsZero`, TD-206); guard idempotensi `generate_picking_from_sp` direvisi supaya picking `done` yang SJ-nya **sudah pernah berangkat** tak lagi memblokir picking lanjutan. Rantai hilir (`complete_picking`/`generate_delivery_from_picking`) **tak diubah** — memang sudah partial-capable sejak awal. Detail: `05_WORKFLOW_MAP.md` §Partial picking; konsekuensi disengaja: TD-206 / Keputusan Terbuka #31. |
| | Harga kategori produk (semester/tahunan/project) | ✅ | FASE 0 (`set_product_category_prices`) |
| | Sales Order legacy — flag finance per item INV/FP/SUB/KRM | ⏸ | flag lama, **bukan** sumber status (lihat `05` §USANG) |
| | AR / TTF (`ar_ttfs`/`ar_btbs`) | ✅ | db.js (legacy Storbit, domain finance terpisah). **[8 Agu 2026]** `ar_ttfs` +2 kolom FK (`sp_order_id`/`invoice_id`) — **LIVE**, dikonfirmasi `schema_snapshot.sql` refresh (kolom+FK constraint ADA) — TANPA backfill DATA historis (step M7 `DESIGN_SP_SCHEMA.md`, belum dikerjakan; snapshot schema-only tak bisa membuktikan isi baris) |
| **Inventory / Warehouse** | Stok Barang, Penerimaan Barang (→ stock_ledger), Inventory Dashboard | ✅ | 2.0D/E, 2.8N |
| | Master Item / Kategori | ⏸ | redirect ke Stok Barang |
| **Asset Management** | IT/Kendaraan/Furniture/Properti (list + detail), Dashboard, Add Asset wizard, inline-edit IT | ✅ | dalam grup Service Management (2.7A); save wizard masih dummy |
| | Documents / Work Orders / Routes | 📋 | tabel belum ada (TD-26) |
| **HRGA Request** | Request (ATK form, My/All Requests, Detail, approval matrix) | ✅ | staging verified (2.0A) |
| | Offboarding | 📋 | tabel ada, UI belum |
| **Service Management** | IT Service Management (ticketing) | 📋 | |
| **Finance** | COA, Cost Centers, Currencies, Exchange Rates, Taxes | 🔄 | tabel ada; UI Finance defaults (Admin Settings) ✅, modul transaksi belum |
| | Billing/Invoice, AR Collection, AP, Cash/Bank, Accounting | 📋 | **Modul AR/AP umum LINTAS-domain, genuinely belum dibangun.** ⚠️ **Jangan tertukar** dgn baris "Logistics (Storbit SP)" di atas — `sp_invoices` (FASE 4, 8 Agu 2026) **SEMPIT**, scoped ke fulfillment Storbit SP saja (1 invoice per SP), BUKAN modul Finance umum ini |
| **Procurement** | **PRF (Price Request Form)** — form + child fields dinamis + menu + nomor auto (Fase 1+2) | ✅ | tabel `prf` (FASE 0) + `PRFFormPage` (Section 01/02/03/04); sales/gm_bd bikin, procurement lihat. **Fase 2 (child fields Sea/Air/Inland/Custom/Project + add-on 11 opsi) done.** List/inbox (Fase 3a) + cross-entity (Fase 3b) belum. **Belum tes manual runtime.** **[KOREKSI 25 Jul 2026 — baris ini belum menyebut batch field B1]** 23 Jul 2026 (commit `1235f4a`, 13:49) — +field **Nama Barang** (`prf.goods_name`, prefill dari `inquiries.goods_name`) · +**UN Number**/**IMO Class** saat `commodity='dg'` (`prf.un_number`/`imo_class`) · enum `commodity` PRF +4 opsi (`perishable`/`live_animal`/`valuable`/`pharma`, form-only tanpa CHECK) · Cargo Ready Date jadi opsional · **Incoterm jadi wajib saat Submit** · Volume Air jadi opsional (Volume Sea-LCL tetap wajib). SQL prasyarat (3 kolom nullable) dijalankan manual 23 Jul, direkam migrasi arsip `20260725000005` (25 Jul). Detail: `PROGRESS.md` 2026-07-25. **⭐ [27 Jul 2026 — fondasi model award BARU "Penawaran Vendor", DB-only, `190fb5e`+`5c87119`]** Model award DIBALIK: procurement kini menyediakan opsi (tabel baru `prf_vendor_offers`, banyak baris per PRF, `pros`/`cons` NOT NULL), **sales yang memilih** (`prf.selected_offer_id`, via RPC baru `prf_select_offer`) — menggantikan model lama `prf_cost_items.is_awarded` — ~~**TD-122 RESOLVED, usang** — kolom lamanya tetap ada, tak di-drop~~ **[KOREKSI Den 27 Jul 2026 — SALAH, TD-122 TETAP OPEN]** kolom model lama (`vendor_id`/`item_group`/`is_awarded`) tetap ada & TAK di-drop, panel Jawaban Harga live masih 100% memakainya, dan lubang double-award yang jadi akar TD-122 **masih hidup di produksi**; baru gugur di Fase 4 (`is_awarded` DAN `prf_cost_items.prf_id` dibuang — kutipan Den, kenapa `prf_id` ikut perlu konfirmasi). +4 RPC SECURITY DEFINER (`prf_claim`/`prf_release`/`prf_mark_quoted`/`prf_select_offer`) menjaga: satu procurement pengerja per PRF (claim/release, TANPA takeover), minimum 3 penawaran dengan jalan keluar tertulis (`min_offers_waiver_reason`). ~~**⚠️ UI = NOL** (nol pemanggil FE, murni fondasi DB)~~ **[KOREKSI 27 Jul 2026 sore — SEBAGIAN USANG, "Batch 3A"]** `PRFDetailPage.jsx` (2 file FE, `feat/prf-audit`, belum di-commit) kini punya tombol **"Ambil PRF Ini"/"Lepas PRF"** (`prf_claim`/`prf_release`) + kartu baca **"Penawaran Vendor" READ-ONLY** (`prf_vendor_offers` SELECT) — UI PERTAMA modul ini. **Cakupan TETAP SEMPIT:** `prf_mark_quoted`/`prf_select_offer`/~~form tambah-edit `prf_vendor_offers` MASIH nol pemanggil FE~~ **[KOREKSI 27 Jul 2026, sesi terpisah SETELAH Batch 3A — Batch 3B]** form tambah/edit/hapus **SUDAH ADA** (`PRFVendorOfferModal.jsx`, 3 file FE, gate `canManageOffers` = HANYA pemegang PRF sendiri, tanpa fallback manager) — ~~`prf_mark_quoted`/`prf_select_offer` **TETAP** nol pemanggil FE, panel Detail Inquiry sales (3C) **TETAP** nol UI — baris "Next Up" di bawah **BELUM otomatis terselesaikan**, hanya sepotong lebih besar dari sebelumnya.~~ **[KOREKSI 27 Jul 2026, sesi terpisah SETELAH Batch 3B — Batch 3C, 4 file FE `feat/prf-audit`]** Ketiganya **KINI ADA**: tombol "Nyatakan Penawaran Siap" (`prf_mark_quoted`) + tombol "Pakai/Ganti Penawaran Ini" (`prf_select_offer`, `DealDetailPage.jsx`) + panel Detail Inquiry sales menampilkan penawaran vendor read-only (`PrfListCard`/`OfferMiniCard`, `DealPanels.jsx`, opt-in — `CustomerDetailPage.jsx` tak terpengaruh). Dasar hitung "Buat Quotation" juga pindah ke `selected_offer_id` → **TD-156 RESOLVED (kode)**. **Transisi `CANCELLED`/`EXPIRED` juga BELUM ADA** (nol RPC, sengaja ditunda) — **TD-154** (baru, TAK BERUBAH oleh Batch 3C). **+TD-155** (LOW — mirror `is_manager_or_above()` di `ActivitiesPage.jsx` ketinggalan `gm_bd`, ditemukan saat batch ini). **+TD-156 (baru, MEDIUM, dari Batch 3B)** — kalkulasi "Buat Quotation" masih basis `is_awarded` (model LAMA); Batch 3B menutupnya sementara lewat disiplin `is_awarded=false` eksplisit di jalur tulisnya sendiri (`offer_id`+`prf_id` juga wajib diisi tiap baris `prf_cost_items` baru, RLS mencari induk lewat `prf_id`), TANPA penegakan DB — **PRASYARAT WAJIB** (instruksi eksplisit Den) sebelum dasar hitung dipindah ke `selected_offer_id` di Batch 3C **[SUDAH DIPENUHI — lihat koreksi TD-156 RESOLVED di atas]**. Hapus penawaran **DIBLOKIR** saat sedang jadi `prf.selected_offer_id` (keputusan bisnis: dasar harga jual quotation yang mungkin sudah dikirim sales, tak boleh dicabut sepihak). Detail: `PROGRESS.md` 2026-07-27 (entri Batch 3A + Batch 3B + Batch 3C) + `03_DATA_MODEL` entri `prf_vendor_offers` + `05_WORKFLOW_MAP` §Procurement — PRF (subbagian "Penawaran Vendor") + `08_TECH_DEBT` TD-109 (usulan turun MEDIUM→LOW DITARIK KEMBALI oleh Batch 3C, tetap MEDIUM)/TD-122 (REOPENED 27 Jul — lubang double-award masih live, TAK BERUBAH oleh Batch 3C)/TD-154/TD-155/TD-156 (RESOLVED kode, Batch 3C). |
| | PR, PO, Vendor Mgmt | 📋 | tabel `vendors` ada |
| **Approval Center** | Reusable approval engine | 🔄 | tabel + Admin Settings UI ada; engine runtime belum |
| **BNF / Briefing Harian / Meeting Mingguan** [Layer 0-4 BARU, 10 & 12 Agu 2026] | Eskalasi insiden (BNF, fondasi 3-5 Agu) + entry harian task/aktivitas/insiden (Briefing Harian) + rollup mingguan per departemen (Meeting Mingguan) | ✅ (kode+SQL live) | Gate akses baru `is_bnf_authorized()` (super_admin/head/direktur/role manager-ke-atas/grant `bnf_authorized_users`); BNF gate dibalik dari `public` jadi `is_bnf_authorized()`; Briefing Harian TETAP `public`. **NOL tes runtime independen doc-keeper** — Den lapor smoke test Passed. +11 TD baru (TD-184…TD-194). Detail: `03_DATA_MODEL.md`/`05_WORKFLOW_MAP.md` §BNF + Selesai Terbaru (10 & 12 Agu 2026) di atas. |
| **Document Mgmt / API / Portal / Reporting / Audit** | — | 📋 | arah jangka panjang (`AGENTS.md`) |
| **App Launcher** | Bento module grid + permission gating | ⛔ **DIHAPUS 7 Agu 2026** | 2.0H. **[7 Agu 2026]** `src/modules/launcher/AppLauncher.jsx` (410 baris) dihapus — diverifikasi **orphan total, nol importer** di seluruh repo sebelum dihapus (bagian retirement sistem `role_permissions` legacy, Tema 2). Baris ini dipertahankan sbg jejak historis, bukan dihapus dari tabel ini. |

---

## Selesai Terbaru (5 Sep 2026 — Dashboard Storbit: Laporan Per Barang + strip nilai/outstanding + export Excel/PDF + pecah jadi 3 tab)

- ✅ **2 migrasi, keduanya LIVE di produksi 5 Sep 2026, 100% BACA.** `20260905000001` = **4 RPC baru** (`get_storbit_product_report` / `get_storbit_product_sp_list` / `get_storbit_outstanding_summary` / `get_storbit_top_outstanding_products`) + index `idx_sp_items_product_id`. `20260905000002` menyentuh **3 dari 4** fungsi itu (2 `CREATE OR REPLACE`, 1 DROP+CREATE karena `RETURNS TABLE` berubah) untuk menambah **satuan produk** + kartu **Nilai Total SP**. **NOL DDL tabel, NOL perubahan RLS, NOL backfill.**
- ✅ **Dashboard Storbit dipecah jadi 3 tab** — Shipping Manifest / Gudang / Laporan Per Barang. Strip 4 kartu nilai berada **DI ATAS** tab (selalu tampil di ketiganya). **Lazy-fetch:** `get_storbit_top_outstanding_products` baru dipanggil saat tab 3 pertama kali dibuka, hasilnya di-cache di state — pindah tab bolak-balik **tidak** memicu fetch ulang (`tabLaporanDibuka` sekali `true` tak pernah balik `false`). FE-only, nol migrasi.
- ✅ **Export Excel + PDF.** Dependency BARU **`exceljs ^4.4.0`**, dipanggil lewat **dynamic import** supaya nol beban di chunk utama — terbukti: chunk `exceljs` **929,92 kB** terpisah, chunk halaman tetap **~41 kB**. PDF: `StorbitReportPDF.jsx` (palet ungu/krem lewat `printTokens.js`, **orientasi landscape** — menyimpang dari 3 dokumen Storbit lain karena tabel daftar SP punya 11 kolom yang pasti terpotong di portrait).
- ✅ **Angka acuan terverifikasi di produksi 5 Sep 2026** (dipakai sbg patokan anti-drift; kalau salah satu bergeser setelah perubahan, ada yang salah): **Nilai Total SP** 514 SP / **Rp 9.612.991.488,46** (BRUTO) · **Outstanding Kirim** 86 SP / **Rp 1.887.625.632** (DPP) · **Outstanding Tagih** 422 SP / **Rp 6.273.578.117,31** (DPP) · **Outstanding Piutang** 5 invoice / **Rp 47.293.798** (BRUTO) · produk unik yang pernah muncul di SP = **38**.
- ✅ **Koreksi dokumentasi yang basi:** klaim palet PDF di `03_DATA_MODEL.md` **TERBALIK** — `PickingListPDF`/`DeliveryNotePDF`/`InvoicePDF` kini **satu keluarga** ungu `#5b3fa0`/krem `#f6f4f1`/Lora+Cormorant lewat `printTokens.js`; navy/orange adalah brand cetak **CRM**, bukan Storbit. Sudah diperbaiki.
- ⚠️ **Tech debt: +TD-217 (HIGH) / TD-218 (HIGH) / TD-219 (LOW) / TD-220 (LOW).** Dua yang HIGH adalah **bug akses yang sedang dirasakan user**, bukan kebersihan — keduanya ketahuan dari pemakaian nyata akun Finance, bukan audit. **TD-24 dikoreksi** (RPC ber-`anon` 3 → 2). **TD-180 tetap PARTIAL** + bertambah satu instance konkret.
- ⚠️ **Cara `pg_dump` snapshot BERUBAH** jadi `--schema-only --schema=public`: 133 tabel `public` **utuh** (nol hilang), tapi **seluruh 133 blok `COPY` lenyap** dari file — yang saat itu **bertentangan** dengan `02_RULES_GOVERNANCE.md` §4. ✅ **DIPUTUSKAN 6 Sep 2026 — tetap schema-only**; §4 sudah diselaraskan dan kontradiksinya ditutup (Keputusan Terbuka #32). Detail: `03_DATA_MODEL.md` **gotcha #23**.
- ✅ **Diverifikasi doc-keeper sendiri:** keempat RPC + ACL-nya ADA di `schema_snapshot.sql` · `npm run lint` = **170 problems (148 errors, 22 warnings)** · policy `prospects_read` `:17354` & `sp_invoices_read` `:17867` dibaca langsung (dasar TD-217/TD-218) · `npm audit --omit=dev` = 2 moderate lewat `exceljs > uuid` (dasar TD-220) · hitung tabel snapshot 133 ↔ 133 lintas commit.
- **[RELAY]** `npm run build` clean 2623 modules + checklist manual tab/lazy-fetch/PDF **LOLOS** = klaim Den; doc-keeper nol akses browser. ⚠️ **Tiga perbaikan kosmetik terakhir BELUM ditest runtime**: hapus 3 kicker, pindah kotak cari SP ke tab 1, ratakan margin puncak tab 3.

## Selesai Terbaru (18 Agu 2026 — Dashboard Storbit + 2 bug fix incidental halaman Produk)

- ✅ **Skema:** `products.reorder_point` + `sp_orders.price_category` (+CHECK +`GRANT UPDATE` per-kolom). **Keduanya sengaja NOL backfill** — `reorder_point` menunggu angka SOP Storbit, `price_category` NULL untuk seluruh SP existing (tipe SP historis tak diketahui sistem).
- ✅ **3 RPC baru** (`get_storbit_dashboard_stats` / `get_storbit_sp_drilldown` / `get_storbit_stock_drilldown`) — SECURITY INVOKER + STABLE, ACL pola FASE 5 (`REVOKE … FROM PUBLIC` + `GRANT … TO authenticated`, nol `anon`), + 2 index.
- ✅ **Halaman `StorbitDashboardPage.jsx`** + wiring 5 titik `App.jsx` + dropdown "Tipe SP" di Input SP.
- ✅ **Redefinisi `expired_date`** jadi tenggat wajib kirim/risiko pinalti (bukan validitas harga) + metrik `pernah_risiko_pinalti` dengan cakupan transparan (**16,2%**, wajib tampil berpasangan penyebut).
- ✅ **2 bug fix incidental** halaman Produk (overflow modal Detail Produk, key React duplikat di list) — **keduanya PRE-EXISTING**, `ProductDetailPage.jsx` terakhir disentuh 7 Jul 2026.
- ⚠️ **Tech debt:** TD-178 +1 kasus (kini 3 titik), +TD-200, +TD-201, +TD-202; TD-199 dikoreksi (lunas).
- ✅ **Diverifikasi doc-keeper sendiri:** snapshot segar (3 RPC + 2 kolom + 2 index ada di `schema_snapshot.sql`) + `npm run lint` **171/149/22 = net-zero**.
- **[RELAY]** `npm run build` clean 2616 modules = klaim main agent. Seluruh tes end-to-end browser (kartu cocok RPC, drill-down navigasi benar, DevTools overflow & warning key) = **klaim Den** — doc-keeper maupun main agent nol akses browser sesi ini.

## Selesai Terbaru (16-17 Agu 2026 — Storbit SP FASE 5: pembayaran/LUNAS + jurnal AR minimal + TTF, redesign visual Detail SP, gate anti double-entry `ar_btbs`)

✅ **FASE 5 — LUNAS: SQL + UI SEMUA LIVE.** 2 tabel baru `journal_entries`/`journal_entry_lines` (jurnal AR **minimal, auto-post tanpa approval**, cuma 2 `reference_type` yang dikunci CHECK: `invoice_issued`/`payment_received`; tanpa `updated_at` — koreksi = jurnal pembalik; CHECK `sign_check` menolak baris debit+credit sekaligus) · `sp_payments` +2 kolom bukti potong PPh 23 (URL manual ke Drive/Storage, **interim** — belum ada upload terintegrasi) · `ar_ttfs` +`diterima_oleh` (text bebas, **sengaja BUKAN FK `profiles`** — orang di pihak customer) + 2 index FK · 2 RPC baru **`record_payment`** (otorisasi **lebih ketat**: super_admin/finance_controller saja, `is_manager_or_above()` sengaja dikecualikan) & **`mark_ttf_received`** (**UPSERT**, otorisasi lebih longgar) · `sp_recompute_status` + `create_invoice` di-`CREATE OR REPLACE` (cabang LUNAS di puncak CASE; blok jurnal 4 baris saat invoice terbit). UI: form Terima Pembayaran + Riwayat Pembayaran + Tanda Terima Faktur **inline di dalam card Invoice** (bukan card mandiri) + helper `db.js` `recordPayment`/`markTtfReceived`/`getPaymentHistory`/`getTtfStatus`. **Seluruh klaim SQL diverifikasi doc-keeper langsung ke `schema_snapshot.sql` pasca-refresh (commit `35cb1d3`), body fungsi dibaca penuh.**

✅ **Redesign visual Detail SP** (`SalesOrderDetailPage.jsx` 1599 → **2065 baris**, angka diverifikasi via `git show`; +file CSS module baru `salesOrderDetail.module.css`): ungu Storbit menggantikan navy/amber/merah untuk **seluruh badge status halaman ini** (navy/amber/merah tetap hidup di elemen non-badge — stat card, MiniBar, modal hapus), tipografi Cormorant/Lora, sidebar "Preview Dokumen" naik dari kolom ke-3 grid Overview jadi **sidebar level halaman (persisten di semua tab)**, vokabular badge **3 varian** (pale/outline/neutral) + **pengecualian oranye khusus warning** (stok kurang, invoice belum terbit) = 4 warna semantik, `Badge` jadi rounded-rect tipis (BUKAN pill) lewat satu komponen yang dipakai seluruh halaman. Card grid Overview yang ADA sekarang: SP Date & Expired · Progress Pengiriman · Financial Summary · Finance Status · Invoice · BTB Numbers (+ Preview Dokumen sbg sidebar).

✅ **Security fix — celah double-entry `ar_btbs` vs `sp_payments` ditutup (2 lapis, level aplikasi, sengaja BUKAN trigger DB):** gate `btbLocked` di `ARModal` (`App.jsx`) + guard `invoice_id` di `updateTtf` (`db.js`, dibaca dari DB bukan state klien). **Blast radius NOL data existing** (`ar_ttfs`/`ar_btbs` dilaporkan kosong — klaim COUNT Den).

✅ **Tes end-to-end DILAPORKAN LOLOS (klaim Den, DI-RELAY — bukan verifikasi independen doc-keeper):** SP dummy satu siklus penuh Draft → Confirmed → Picking → Delivered → Invoice → Payment parsial → Payment lunas → SP naik LUNAS → TTF; jurnal balance (saldo Piutang net 0 setelah lunas); cleanup total, stok kembali persis ke angka semula. Lompatan besar dari FASE 4 yang berstatus "NOL tes runtime" — tapi tetap laporan, bukan bukti yang doc-keeper bisa reproduksi.

✅ **Disiplin migrasi:** `20260817000001_fase5_jurnal_ar_payments_ttf.sql` (**RETROAKTIF** — SQL jalan manual dulu, file ditulis dari isi snapshot; **bukan untuk di-run ulang**; penomoran **STEP 1–9 lokal, sengaja BUKAN M1–M13** supaya tak bentrok tabel rencana `DESIGN_SP_SCHEMA.md` §5 yang isinya berbeda) + **snapshot sudah di-refresh** (bukan utang). Migrasi terakhir sebelumnya: `20260814000003_invoice_due_date.sql`.

⚠️ **TD:** TD-176 **PARTIAL** (tertutup hanya utk `sp_payments`) · TD-180 **PARTIAL** · **+TD-197** (casing `dppPPN`/`dppPpn`, MEDIUM) · **+TD-198** (kolom mati `sp_orders.confirmed_at/by`, LOW) · **TD-178 tetap OPEN** (hardcode UUID SOA di `sp_recompute_status` — diverifikasi masih ada, sengaja tak disentuh).

⚠️ **BELUM dikerjakan — 4 item yang SEMPAT salah dilaporkan selesai di sesi sebelumnya, dikoreksi lewat audit read-only 17 Agu 2026 dan diverifikasi ulang independen oleh doc-keeper (grep, 0 hit semuanya):** (1) **5 card baru grid Overview BELUM ADA** — Ringkasan Pesanan, Kontak Pelanggan, Catatan Internal, Dokumen Terkait, Aktivitas Terbaru; (2) **instrumentasi `logAudit` di titik lifecycle SP BELUM ADA** (confirm/cancel/generate picking/dispatch/terbitkan invoice — `logAudit` dipakai 7 modul lain, tapi 0 hit di `SalesOrderDetailPage.jsx`); tab History masih `EmptyState` placeholder menunggu ini; (3) **badge Tersedia/Overdue per-item di tabel Items BELUM ADA** — yang ada cuma memo `stockShort` yang merender badge "Stok Kurang" di area header dan hanya saat `canGeneratePicking` (itu fitur FASE 1 lama, bukan badge sub-baris); kolom "Akun" & "Diskon" tabel Items masih hardcode "—" (belum ada sumber data / tak ada kolomnya di skema); (4) **tracking delivery telat outbound (SLA/vendor accountability)** baru tahap investigasi, NOL skema/RPC. **Doc-keeper mengecek: tak ada dokumen Governance/PROGRESS yang terlanjur menandai keempatnya selesai** — jadi tak ada koreksi retroaktif yang perlu ditulis.

Verifikasi independen doc-keeper lainnya: `npm run build` **clean, 2614 modules**. Detail: `PROGRESS.md` 2026-08-17 · `03_DATA_MODEL.md` §Inventory & Logistics + §5 · `05_WORKFLOW_MAP.md` §Logistics.

---

## Selesai Terbaru (10 & 12 Agu 2026 — BNF extension 4-lapis: Briefing Harian + BNF diperluas + Meeting Mingguan + Akses BNF Tambahan)

**Layer 0-4, dikerjakan 10 & 12 Agu 2026 (jeda 1 hari tanpa commit, 11 Agu).** Layer 0 (fondasi DB, 10 Agu): 7 tabel baru (`bnf_authorized_users`, `bnf_division_scopes`/`bnf_department_scopes` — model home+scope, `daily_report_items`, `bnf_report_action_items`, `weekly_meetings`/`weekly_meeting_items`) + fungsi gate `is_bnf_authorized()` (SQL STABLE SECURITY DEFINER) + RLS `bnf_reports_select`/`_insert` diperketat ke gate itu. Layer 1 (12 Agu): halaman baru **Briefing Harian** (`public:true`, terbuka semua staff — task/aktivitas/insiden harian + auto carry-forward task pending). Layer 2 (12 Agu): BNF diperluas — gate menu dibalik dari `public:true` jadi `is_bnf_authorized()`, tab baru "Tarik dari Insiden" + checklist Action Items. Layer 3 (12 Agu): halaman baru **Meeting Mingguan** (gate sama BNF) — susun agenda mingguan dari 3 sumber (task pending/insiden selesai internal/laporan BNF aktif). Layer 4 (12 Agu): section baru "Akses BNF Tambahan" di `BNFOrgRolesPage.jsx` — grant/revoke akses BNF di luar jalur struktural, super_admin-only. `is_bnf_authorized()` **diperluas 12 Agu 2026** dari `ceo` saja ke `ceo`/`gm`/`gm_bd`/`manager`/`finance_controller` (rollout SOP BNF ke semua staff). 2 bug produksi ditutup (badge "Jadi BNF" salah tampil nomor; default filter Overview BNF sempat menyembunyikan laporan Closed). +11 tech debt baru **TD-184…TD-194** (`08_TECH_DEBT.md`).

✅ **Seluruh SQL dikonfirmasi live** (6 migrasi `20260810000001`-`20260811000002`, `schema_snapshot.sql` fresh, GRANT lengkap 7/7 tabel baru). **NOL tes runtime independen doc-keeper** — Den melaporkan smoke test penuh Passed (di luar kapasitas verifikasi sesi dokumentasi). **Governance docs 03/04/05 sebelumnya nol dokumentasi modul BNF** (termasuk fondasi 3-5 Agu 2026) — kini net-new. Detail: `03_DATA_MODEL.md` §BNF · `05_WORKFLOW_MAP.md` §BNF Flow · `04_ROLE_PERMISSION_MATRIX.md` §Peta Menu · `PROGRESS.md` 2026-08-10 & 2026-08-12.

---

## Selesai Terbaru (9 Agu 2026 — Access Control fixes + multi-entity plumbing Fase 1 + AdminHub + CompanySwitcher + PPN consolidation)

**9 perubahan, satu sesi panjang — access control/RBAC + fondasi multi-entitas + restrukturisasi Admin.**

- ✅ **Access Control — 3 gap ditutup (audit read-only sesi ini → langsung fix).** Redirect legacy `'users'` landing TEPAT di card User Access (bukan default landing hub). Tab "Permissions" `UserEditPage.jsx` digate `isSuperAdmin` (dulu tampil+bisa diklik utk `admin` biasa meski RLS `user_menu_permissions` cuma izinkan tulis `super_admin` — trap-UX). Picking List & Surat Jalan masuk sistem `hasMenuPermission`/`role_menu_permissions` granular (dulu murni `role[]` hardcode, migrasi `20260809000001`). Property `permission` mati dihapus dari `AdminShell.jsx`; entry `MENU_KEY_MAP['adminSettings']` camelCase (dead code, tak pernah ke-hit) dihapus.
- ✅ **Multi-entity plumbing Fase 1 (`activeCompanyId`).** `AuthContext.jsx` expose `activeCompanyId`/`setActiveCompanyId` (session-only)/`myCompanyIds`; `pickPrimaryErpRole` & tier-3 `hasMenuPermission` scoped ke company aktif (dulu lintas-company tanpa filter). Fungsi SQL baru `get_user_company_ids()` (jamak, beda dari `get_user_company_id()` singular yang tak disentuh). Konsumen: `hooks/useProducts.js`/`useAppSettings.js`.
- ✅ **Fix RLS bypass privilege-escalation.** `roles_insert`/`roles_update`/`user_roles_insert`/`user_roles_update` diperbaiki jadi bypass `is_super_admin()` TOP-LEVEL yang benar (sebelumnya `user_roles` versi TD-170 menaruhnya nested di dalam `AND` — bahkan super_admin terkunci home company sendiri; `roles` sebelumnya malah tanpa bypass sama sekali).
- ✅ **AdminHub — unifikasi Master Data + Admin Settings.** `AdminShell.jsx`+`AdminSettingsHub.jsx`(+9 sub-halaman) melebur jadi satu `AdminHub.jsx` (25 destinasi, 6 grup, gating 2-lapis replikasi persis kondisi asli). File lama jadi orphan (dibiarkan di disk). Redirect legacy digeneralisasi dari 1 id (`'users'`) jadi 12 id.
- ✅ **CompanySwitcher — switcher company aktif topbar.** `src/components/CompanySwitcher.jsx` ganti tombol dekoratif mati "MSI/JCI/Storbit" (**TD-92 RESOLVED**). Session-only, filter `myCompanyIds`. **Diuji dgn user multi-company asli** (Elvira Nurhuda) — menyingkap + (terpisah) menutup RLS gap `companies_read_own`/`roles_read`.
- ✅ **Payment Terms jadi FK.** `entity_finance_settings.default_payment_term_id` (baru) — `FinanceDefaultsPage.jsx` dropdown ganti input angka manual; `getInvoicePdfData` resolusi 3-tingkat (FK→kolom lama→30). Kolom lama dipertahankan, berhenti ditulis.
- ✅ **PPN rate consolidation.** `src/lib/taxConstants.js` (`PPN_RATE=0.11`, `PPN_RATE_FREIGHT_FORWARDING=0.011` — beda SENGAJA, bukan bug) — 4 file konsumen disambungkan, ganti 5 titik hardcode literal.
- ✅ **Fix bug produksi — dropdown VAT Quotation.** PPh21/PPh23 (rate>1) sempat ikut ter-union ke dropdown PPN, tertampil "500%"/"200%" — kalau terpilih, PPN quotation 200-500× lipat. Ditemukan via audit data live. Fix: filter `tax_type='percentage' AND rate<=1`.

**Pola berulang ditemukan (WAJIB diaudit lanjut, bukan cuma catatan sejarah):** RLS `is_super_admin() OR company_id=get_user_company_id()` (home company tunggal) TIDAK CUKUP untuk user genuinely multi-company — ditemukan berulang 3× satu hari (`companies`✅/`roles`✅/`payment_terms`❌ masih OPEN), lewat testing/audit terpisah tiap kali, bukan satu sisir menyeluruh. Fix yang benar: `OR company_id IN (SELECT get_user_company_ids())`. → `08_TECH_DEBT.md` **TD-180** (baru, OPEN).

> ⚠️ **NOL tes runtime penuh** utk sebagian besar area (kode "build clean" + verifikasi via `schema_snapshot.sql` cross-check, bukan klik di browser) — KECUALI CompanySwitcher, yang sempat diuji dgn user multi-company nyata dan langsung menyingkap bug RLS. Repo sempat berada di tengah **git merge dengan 1 konflik trivial** (`schema_snapshot.sql`, cuma token sesi `pg_dump`) saat sesi ini didokumentasikan — cek `git status` sebelum mempercayai isi snapshot mentah-mentah kalau masih terjadi. Migrasi tercatat sesi ini **hanya** `20260809000001_picking_surat_jalan_menu_permissions.sql` — perubahan RLS/kolom lain belum direkam migrasi. TD baru: **TD-179** (bug PPh dropdown, RESOLVED) · **TD-180** (pola RLS multi-company, OPEN) · **TD-181** (orphan AdminHub, OPEN). TD lama ditutup: **TD-92** (topbar entity selector, RESOLVED). Detail penuh: `PROGRESS.md` 2026-08-09 + `03_DATA_MODEL.md` §RBAC/§Foundation + `04_ROLE_PERMISSION_MATRIX.md` §FOUNDATION/§LOGISTICS + `08_TECH_DEBT.md` TD-92/179/180/181.

---

## Selesai Terbaru (8 Agu 2026 — Storbit SP Fase 4 Invoice + DC Master CRUD + DcPicker + RLS GRANT hardening + dual-write trilogy + InvoicePDF)

**6 area saling terkait, satu sesi panjang.**

> **⚠️ KOREKSI SUSULAN SAMA HARI (malam):** bullet-bullet di bawah awalnya ditulis saat `schema_snapshot.sql` MASIH basi dan menandai area Invoice/RLS-GRANT/dual-write "SQL belum dijalankan" — itu bacaan jujur thd bukti saat itu. Snapshot kemudian di-refresh (commit `8117127`, 23:33 WIB, +416 statement `GRANT`) dan doc-keeper memverifikasi ULANG presisi langsung ke isinya — **hasilnya: SEMUANYA LIVE**, kecuali TD-176 (tetap OPEN) dan backfill data `ar_ttfs` (M7, status tak berubah). Bullet di bawah sudah dikoreksi mengikuti hasil verifikasi ulang ini.

- ✅ **DC Master jadi CRUD penuh** (dulu read-only). Halaman admin baru `DcMasterPage.jsx` + hook `useDcMaster.js` (`AdminShell` › Configuration, gate identik page Configuration lain — TANPA gate baru). 45 DC awal berasal dari migrasi otomatis FASE 0.
- ✅ **DcPicker** — field "Distribution Center" `InputSPPage` diganti combobox searchable (pola `AccountPicker`) menggantikan `<select>` native 45+ opsi; perilaku filter per-customer TAK berubah.
- ✅ **FASE 4 Invoice (INVOICED/SUBMITTED) — KODE+UI+SQL SEMUA LIVE.** 3 tabel baru `sp_invoices`/`sp_invoice_lines`/`sp_payments` (`DESIGN_SP_SCHEMA.md` §2.5, migrasi step **M2** — pembuatan tabel saja; `sp_payments` dibuat tapi FASE 5/payment BELUM dibangun) + `ar_ttfs` +2 kolom FK (`sp_order_id`/`invoice_id`, kolom+FK constraint LIVE — **TANPA backfill DATA** — step **M7**, belum dikerjakan, tak diverifikasi ulang di sini) + RPC baru `create_invoice`/`submit_invoice` + `sp_recompute_status` (`CREATE OR REPLACE`) dapat cabang INVOICED/SUBMITTED **DAN fix bug guard nyata** (guard lama menahan `INVOICED`/`SUBMITTED` di daftar freeze-nya sendiri → status macet permanen di INVOICED, tak bisa naik ke SUBMITTED — ditemukan **Den**, bukan doc-keeper; fix **dikonfirmasi live**, dibaca langsung dari `schema_snapshot.sql`). Kartu "Invoice" baru `SalesOrderDetailPage.jsx` (Terbitkan/Submit/Download PDF). Kode entitas invoice **dikonfirmasi diresolusi dinamis dari `companies.code`** saat runtime — sempat diduga hardcode "SBI" pertengahan sesi, diinvestigasi & dikoreksi, kini diverifikasi doc-keeper langsung ke body RPC live. **⚠️ NOL tes runtime/UI** — belum ada laporan klik tombol sungguhan di browser.
- ✅ **RLS/GRANT hardening — TD-175 RESOLVED, dikonfirmasi live.** Ditemukan celah `authenticated` bisa `UPDATE sp_orders.status` LANGSUNG lewat PostgREST, membypass total mesin status 12-tahap. Fix: REVOKE UPDATE penuh `sp_orders` → GRANT kolom-spesifik — **doc-keeper menghitung ulang persis**: 24/25 kolom ber-GRANT individual, `status` dikonfirmasi 0 hit; `sp_invoices` serupa, 6 kolom dikecualikan **cocok EXACT** dgn desain (`status`/`invoice_no`/`submitted_at`/`total_dpp`/`total_ppn`/`total_amount`). → **TD-175 RESOLVED**. Kolom SEJENIS **TETAP** SENGAJA belum ditambal (diverifikasi ulang, tak berubah oleh refresh): `sp_order_items`/`sp_items`/`sp_invoice_lines`/`sp_payments` — keempatnya `GRANT ALL` (lebih longgar dari dugaan awal "cuma UPDATE") → **TD-176 tetap OPEN** — axis GRANT kolom-level ini BEDA dari axis RLS baris-level TD-173/TD-39.
- ✅ **Dual-write trilogy `sp_items`↔`sp_order_items` — KODE+SQL SEMUA LIVE (termasuk backfill).** `dispatch_delivery`/`cancel_delivery` (`CREATE OR REPLACE`) + RPC baru `update_sp_item_dual` (dipakai `updateSpItem`, `db.js`) — **ketiganya dikonfirmasi live**, dibaca langsung dari body fungsi di `schema_snapshot.sql` pasca-refresh; `update_sp_item_dual` dikonfirmasi dual-write KELIMA kolom overlap (`qty`/`sla_days`/`estimated_delivery_date`/`shipping_price`/`notes`), bukan cuma 2 seperti dugaan awal doc-keeper — pemetaannya benar via baca kode, tapi eksekusi data-uji nyata baru terbukti utk `notes` (skrip ZZZTEST sesi ini), `sla_days`/`estimated_delivery_date` belum. **Backfill satu-kali 115 baris** (1 dikecualikan: SP 2181355 CANCELLED) tetap live seperti semula, kini dilengkapi mekanisme sinambung yang juga live — inilah akar sebab Invoice FASE 4 sebelumnya tak bisa dipakai utk SP lama manapun (`shipped_qty` selalu 0/basi → guard `create_invoice` mustahil lolos). → **TD-40 diperbarui** (LOW→MEDIUM severity, **RESOLVED**).
- ✅ **InvoicePDF** — dokumen baru `@react-pdf/renderer`, identitas visual ungu/krem/serif **sengaja beda** dari Picking List/Delivery Note (navy/orange) — keputusan desain Den. NPWP entitas SOA kosong di DB (data belum diisi, bukan bug). QR code placeholder sengaja kosong (keputusan belum diambil — §Keputusan Terbuka #24 di bawah). 3 bug visual nyata ditemukan+diperbaiki sesi ini (rotasi/clipping label, `react-pdf` mengabaikan `rgba()` di border, text-wrap width) → **TD-177** (baru, LOW — tak ada test regresi visual utk PDF `@react-pdf/renderer`). *(Tak terpengaruh isu snapshot — murni FE.)*
- 🔍 **Ditemukan, TAK diperbaiki sesi ini — DIPERLUAS pasca-verifikasi-ulang:** `sp_recompute_status` hardcode UUID entitas SOA di kalkulasi cek stok MENUNGGU_STOK, **DAN instance KEDUA ditemukan di `dispatch_delivery`** (pola sama, + hardcode warehouse ID) — nol dampak praktis hari ini (modul SP eksklusif SOA) → **TD-178** (baru, LOW, cakupan diperluas).

> ✅ **`schema_snapshot.sql` KINI MEREFLEKSIKAN seluruh area Invoice/RLS-GRANT/dual-write di atas** (`pg_dump` commit `8117127`, 23:33 WIB, 8 Agu 2026) — koreksi atas klaim sebelumnya di sesi yang sama ("nol `pg_dump` sejak sesi ini") yang ditulis sebelum refresh terjadi. **Migrasi TETAP belum direkam** (`supabase/migrations/`) meski SQL sudah live — item administratif tersisa. Detail penuh: `03_DATA_MODEL.md` (banner 8 Agu 2026 + entri `sp_orders`/`sp_order_items`/`sp_items`/`sp_invoices`/`ar_ttfs`/`dc_master` + gotcha #9, SEMUA dikoreksi ulang) + `05_WORKFLOW_MAP.md` (§Logistics Storbit SP) + `08_TECH_DEBT.md` (TD-40/TD-175…TD-178, TD-175 & TD-40 kini RESOLVED) + `PROGRESS.md` 2026-08-08 (entri susulan).

---

## Selesai Terbaru (7 Agu 2026 — RBAC hardening: fix eskalasi role, pensiun sistem permission lama, role-menu-permission Fase B)

**Fix keamanan CRITICAL + rombak fondasi permission — 5 commit terkait, DB + FE:**
- ✅ **Fix eskalasi role `user_roles` (DB live + FE).** RLS `user_roles_insert`/`update` dulu izinkan admin biasa assign `role_id` APA PUN termasuk `super_admin` — privilege escalation murni via RLS. Ditambal fungsi `is_admin_tier_role()` + 2 `ALTER POLICY`; `UserEditPage.jsx` (tombol Save + dropdown role) digate `isSuperAdmin`. → **TD-170 RESOLVED**.
- ✅ **Pensiun sistem `role_permissions` legacy.** `hasPermission()`/`role_permissions` sudah lama ditinggalkan sengaja (komentar developer eksplisit di kode) — sesi ini menghapus tab "Roles" (`AdminShell.jsx`), dead branch fallback `hasPermission` di `canSeeMenuItem` (`App.jsx`, 0 menu yang masih memicunya), dan file orphan `AppLauncher.jsx` (410 baris, 0 importer). `useEffect` fetch `role_permissions` dimatikan di `AuthContext.jsx` — struktur `hasPermission`/`isCrossEntity`/`userPermissions`/`refreshPermissions` DIBIARKAN ada (sengaja vestigial). `RolesPage.jsx` filenya tetap ada tapi unreachable dari UI (referensi masa depan, bukan lupa dihapus). Tabel DB `permissions`/`role_permissions` + fungsi SQL `has_permission()` dibiarkan ada, kodenya berhenti memakai.
- ✅ **Sistem role-based menu permission baru (Fase B).** Tabel baru `role_menu_permissions` (default menu-permission per role, mirror `user_menu_permissions` tapi `role_id`-keyed, TANPA kolom `effect` — role cuma grant, tak ada deny di level role). Kolom baru `user_menu_permissions.effect` (`'grant'`/`'deny'`, default `'grant'`, backward-compatible). `hasMenuPermission` (`AuthContext.jsx`) kini resolusi 3-tingkat: super_admin selalu true > user override (kalau ADA baris cocok di `user_menu_permissions`, menang mutlak sesuai `effect`) > union `role_menu_permissions` dari SEMUA role AKTIF user (union, BUKAN role primer — sengaja menghindari pola `get_user_role_code()` yang terbukti non-deterministic utk user multi-role) > default deny. Halaman baru `RoleDefaultsPage.jsx` (super_admin only, gate di titik render `AdminShell.jsx` — bukan cuma sidebar) + fitur "Salin dari role lain" (destructive replace, dialog konfirmasi eksplisit). `PermissionMatrix` (`userAccessShared.jsx`) dapat mode 3-state OPSIONAL via prop `roleDefaults` (tak di-pass = perilaku identik 2-state lama); dipakai `UserEditPage.jsx` (3-state: ikut role/boleh/larang) dan `RoleDefaultsPage.jsx` (tetap 2-state).
- ✅ **Fix double-write `accounts.pipeline_stage`** (3 jalur tulis, kunci in-flight per akun) — **kode selesai, test A/B lolos, test D/E belum dijalankan**. → **TD-171 PARTIAL** + **TD-172** (gap terpisah, belum ditutup).
- 🔍 **Audit RLS/RBAC menyeluruh** — 19 tabel CRITICAL + gap HIGH/MEDIUM/LOW, seluruhnya BELUM DIEKSEKUSI. → **TD-173** (baru, konsolidasi & perluasan TD-39).

> ⚠️ Migrasi (rekaman): `supabase/migrations/20260807000001_user_roles_block_privilege_escalation.sql` (**snapshot SEGAR** — sudah di-`pg_dump`) + `20260807000002_role_menu_permissions_and_effect_column.sql` (**⚠️ snapshot BELUM di-refresh untuk migrasi ini** — `role_menu_permissions` + kolom `effect` TIDAK ADA di `schema_snapshot.sql` per pengecekan doc-keeper). **Fase B3** (isi data default per role via `RoleDefaultsPage`) dan **Fase C** (satukan menu hardcode `App.jsx` dgn katalog DB `modules`/`module_menus`/`menu_actions`/`module_actions`) — **BELUM DIMULAI**, lihat Next Up. Belum tes manual runtime (kode "build clean" per klaim sesi, belum diverifikasi doc-keeper end-to-end). Detail: `PROGRESS.md` 2026-08-07 + `08_TECH_DEBT.md` TD-170…TD-174 + `03_DATA_MODEL.md` §3 RBAC + `04_ROLE_PERMISSION_MATRIX.md` §4-5.

---

## Selesai Terbaru (10 Jul 2026 — Modul PRF Fase 1+2)

**Procurement greenfield — PRF (Price Request Form):**
- ✅ **Fase 0 (DB, live)** — tabel `prf` (52 kolom, child fields Sea/Air/Inland/Custom/Project sebagai kolom nullable) + trigger `set_prf_updated_at` + 4 RLS policy single-entity. Rekaman: `20260710000001_prf_fase0.sql`.
- ✅ **Fix RLS super_admin (DB, live)** — 4 policy `prf` di-DROP + CREATE ulang, tiap kondisi dibungkus `is_super_admin() OR (…)` (Fase 0 keliru tanpa cabang ini → super_admin ditolak saat INSERT). super_admin kini bypass company scope (LIHAT PRF lintas-3-entitas, standar). BEDA dari cross-entity inbox procurement (Fase 3b, masih ditunda). Rekaman: `20260710000003_prf_rls_super_admin.sql`.
- ✅ **Fase 1 (KODE)** — `PRFFormPage.jsx` (Section 01 Informasi Dasar + 02 Inquiry Details; conditional logic incoterm/DG/domestic; sumber inquiry auto-isi account) + menu `prf` di ERP_MENU_GROUPS/NEXUS_NAV (role[]) + render block + nomor auto `PRF/{ENTITAS}/{TAHUN}/{ROMAWI}/{URUT}`.
- ✅ **Fase 2 (KODE, same file)** — Section 03 "Detail Layanan" child fields dinamis per `service_type` (Sea FCL/LCL + container qty jsonb, Air, Inland 25 armada, Custom PIB/PEB auto, Project); Notes digeser → Section 04; koreksi daftar add-on 6→11 opsi. Ganti `service_type` reset semua child; payload null-out per visibilitas. **Tanpa ubah DB** (kolom sudah ada Fase 0).
- 📋 **Belum:** list/inbox procurement (Fase 3a), cross-entity inbox (Fase 3b). **⚠️ FLAG UX:** Custom butuh 2 syarat (service=custom DAN add-on Custom Clearance) — perlu konfirmasi user testing.

> ⚠️ **Belum tes manual runtime** (perlu login sales/gm_bd). Hanya sales/gm_bd bisa Submit/Draft (RLS). ⚠️ Kolom `inquiry_id` ditambah Den manual — direkam di `supabase/migrations/20260710000002_prf_add_inquiry_id.sql`. `schema_snapshot.sql` STALE. Detail: `PROGRESS.md` 2026-07-10 + `AUDIT_PROCUREMENT.md`.

---

## Selesai Terbaru (FASE 0-3 — Storbit SP mesin status, ~Jul 2026)

Detail granular: `PROGRESS.md` (2026-07-06…08) + `CLAUDE.md` Recent. Skema/alur: `03_DATA_MODEL`/`05_WORKFLOW_MAP`. Semua SQL dijalankan manual (rekaman `supabase/migrations/20260706*…20260708000002`).

- ✅ **FASE 0 — fondasi skema DB:** tabel baru `sp_orders` (header, identitas komposit `(customer_id,sp_no)`, status 12-tahap, `had_cancelled_picking`), `sp_order_items` (kanonik), `sp_btb` (BTB benar), `dc_master`; harga kategori produk `price_semester/tahunan/project`; RLS + backfill (lama=baru). Dual-write InputSPPage.
- ✅ **FASE 1 — mesin status bawah:** `sp_recompute_status` (fact-derived) + tahap DRAFT→CONFIRMED→MENUNGGU_STOK→PICKING→PACKED; RPC picking (generate/complete/cancel) + fix desync.
- ✅ **FASE 2 — jembatan pengiriman:** dispatch/cancel isi `sp_items.shipped_qty`; tahap DIKIRIM/SAMPAI/TERKIRIM_PENUH; `mark_delivery_delivered`. Reader status list pindah ke `sp_orders.status` (2E).
- ✅ **FASE 3 — BTB_TERBIT:** RPC `sp_issue_btb`/`sp_delete_btb` → tabel `sp_btb`; **BTB_TERBIT = rank tertinggi** (mengalahkan TERKIRIM_PENUH — "puncak sebelum invoice"); kartu BTB pindah ke Detail SP; migrasi `sp_btbs`→`sp_btb` (186→205).

> ⚠️ Sebagian "terverifikasi user" (FASE 2C, 3 Step E/G); sisanya "build clean, belum tes runtime penuh". Debt FASE 0-3: `08_TECH_DEBT.md` (TD-38…TD-44).

---

## Selesai Terbaru (24 Jun 2026 — malam, 2.11E)

**Dropdown DB-driven (master data → konsumen form):**
- ✅ **`dropdown_options` table + seed (12 list)** — service_type, unit_label, lead_source, lost_reason, activity_type, customer_type, customer_tier, shipment_mode, container_type, incoterm, leave_type, allowance_type (global; RLS SELECT semua authenticated, write super_admin-only).
- ✅ **Dropdown Management full DB-driven** — CRUD persist ke DB (`dropdown_options` INSERT/UPDATE/soft-DELETE/toggle/reorder); `currencies`+`payment_terms` merge ke Finance (toggle-only, tak ada `sort_order`). Toast sukses + error asli.
- ✅ **`useDropdownOptions` hook** (`src/hooks/useDropdownOptions.js`) — fetch 1 `list_key`, fallback array bila error/empty.
- ✅ **QuotationForm** — `service_type` + `unit_label` via hook (unit pakai label sbg value utk preserve data lama), VAT via fetch `taxes` (rate→label, union dgn fallback agar 0/1,1%/11% selalu ada).
- ✅ **InquiryForm** — `service_type` via hook.
- ✅ **`taxes` dirapiin** — 6 baris duplikat soft-deleted (PPN11/VAT_0 × 3 entitas; sisa VAT_FULL 0.11 + TAXFREE 0). *(dijalankan di SQL Editor, bukan dari repo)*

> ⚠️ 2.11E **build clean, belum tes manual runtime**. Semua const lama dipertahankan sbg `*_FALLBACK`. Writes super_admin-only (RLS) → role lain dapat toast error asli.

---

## Selesai Terbaru (24 Jun 2026)

**Fitur — Admin Settings (port Lovable, 2.11A):**
- ✅ **Security Policy** — password policy, sesi, login protection, 2FA per-role (localStorage fallback `security_policy_*`).
- ✅ **Audit Log** — fetch real `user_login_logs` + join `profiles`; filter/pagination/CSV export (login events only sampai `audit_logs` jadi — TD-37). ⚠️ **Kondisi 2.11A ini kini sudah TIDAK berlaku** — sumbernya diganti ke `audit_logs` di **hari yang sama** oleh 2.11J (`7e40149`); **TD-37 ditutup DONE 17 Jul 2026**.
- ✅ **General Preferences** — lokalisasi/format/tampilan per entitas (localStorage `general_prefs_*`; EntitySwitcher default dari `useAuth`).
- ✅ **Integrations** — WhatsApp/SMTP/n8n webhook/API keys (localStorage `integrations_*`; ⚠️ credentials belum secure — TD-36).
- ✅ **`kit.jsx` extended** — +13 ikon lucide + `KitSelect` (reuse kit existing, tanpa `adminKit.js` baru).

> ⚠️ Semua page 2.11A **"build clean, belum tes manual runtime"**. TD baru: **TD-36** (credentials localStorage — **tetap OPEN**), **TD-37** (AuditLog login-only — **kini DONE**, ditutup 17 Jul; klaim login-only usang sejak 2.11J).

---

## Selesai Terbaru (23 Jun 2026)

**Fitur:**
- ✅ **CRM Report page** — KPI, trend chart, per-sales breakdown, Supabase real data, sidebar menu Report.
- ✅ **Notification bell** — badge, dropdown, mark-as-read, 4 producers (activity assign/done, HRGA submit/approve).
- ✅ **Pending Approval badge** — HRGA pending count, auto-refresh 60s.
- ✅ **Quotation** — currency EUR/SGD/JPY/MYR + VAT rate dropdown + kurs per-baris.

**Tech debt fixes (detail: `08_TECH_DEBT.md`):**
- ✅ `console.*` leak — `AuthContext` 6 dihapus, `ProductsPage` sudah bersih (TD-32; sisa ~65 OPEN).
- ✅ RLS oversight read — 3 policy +`is_manager_or_above` + `is_manager_or_above()` +STABLE (TD-01 PARTIAL).
- ✅ DELETE policy — 4 tabel transaksional: `notifications`, `hrga_request_items`, `hrga_offboarding_items`, `sp_btbs` (TD-03 PARTIAL).

> ⚠️ Beberapa fitur di atas berstatus **"build clean, belum tes manual runtime"** — verifikasi sebelum dianggap final. Perubahan RLS/DELETE: refresh `schema_snapshot.sql` via `pg_dump` bila sudah live di DB.

---

## Milestone Terakhir (terbesar)

1. **Unifikasi `accounts`** — `prospects` + `customers` digabung jadi master customer tunggal (2.2A–C, 2.5A); WON→customer via trigger DB.
2. **Modul Activity terpadu** — `activities`/`activity_logs` gantikan `sales_calls`/`sales_visits`/`sales_visit_logs`; unified Activity Log feed (5 sumber) (2.9B–N).
3. **Overhaul Quotation** — SLA indicator, pricing authority matrix, discount, currency dropdown DB, VAT rate per service_type, dan **PDF rewrite ke `@react-pdf/renderer`** (vector/text) (2.1A, 2.10A–C).
4. **Suite Admin Settings** — Entity/Document/Finance/Approval/Notifications (port desain Lovable + Supabase) (2.6A–F).
5. **Struktur Organisasi (Org Chart)** dari `profiles.reports_to`, warna per-level (2.9S–V).
6. **RBAC menu-permission** — `modules→menus→actions→user_menu_permissions`, gating sidebar + launcher (2.0F+…).
7. **Auth lifecycle hardening** — logout cleanup, restored-menu guard, content-gate, fix tab-switch kehilangan state (2.3D–F, 2.8B).
8. **Deprecate `profiles.role`** — tahap DB functions → Edge Functions → frontend (2.3G–H).
9. **Mobile responsive** — util grid opt-in + nav drawer + kalender/feed reflow (2.8S–Y).
10. **Master Customer page** (list + detail + health score, per-entitas) (2.1C–G).

---

## Branch Aktif (belum merge)

> Branch kerja yang belum masuk `main` — dicatat agar tidak "hilang" dari governance. **Kosong per 17 Jul 2026: tak ada branch kerja yang menggantung — semua sudah merge ke `main`.** Section dipertahankan sbg placeholder untuk pencatatan branch mendatang.

- **`feat/detail-sp-reskin`** (UI-only, 1 file: `src/modules/logistics/SalesOrderDetailPage.jsx`) — re-skin halaman Detail Sales Order dari palet warm/cream → cool/navy brand (navy `#1B4D8A`/orange `#E85A1E`/amber), heading font Space Grotesk (keputusan final — lihat pengecualian di **TD-70**), reposisi header card, + **soft-tone pass** (indigo→slate soft, badge `TERKIRIM_PENUH`/`LUNAS` solid→tint, tombol primer orange solid→soft; tombol DANGER sengaja tetap solid merah). **Status: ✅ SUDAH MERGE ke `main` (`1ff0ffb`)** (koreksi 17 Jul — klaim lama "BELUM merge" **SALAH**); **BELUM tes runtime** (build clean). Detail kronologis: `00_DEV_JOURNEY.md` (Bagian 1, 2026-07-15).

---

## Utang Migrasi Produksi — CRM v3 (per 7 Sep 2026)

> Sembilan migrasi CRM v3 ditulis di branch `feature/crm-v3-batch-persiapan`. **Filenya TIDAK ADA di `main`** — migrasi `main` melompat dari `20260826000003` ke `20260831000001`. Daftar ini ada di sini supaya status produksinya bisa dibaca dari `main` **tanpa membuka branch**, karena `main`-lah yang melayani produksi.

**✅ SUDAH LIVE di produksi — JANGAN dijalankan ulang (4):**

| migrasi | naik | bukti |
|---|---|---|
| `20260827000001_crm_v3_master_data` | 6 Sep 2026 | `loss_reasons` 12 · `channel_types` 9 · `sla_policies` 30 · `inquiries.owner_id` 531/531 |
| `20260830000002_inquiry_owner_backfill_and_lock` | 6 Sep 2026 | `trg_z_lock_inquiry_owner` terpasang |
| `20260828000001_inquiry_status_history` | **7 Sep 2026** | backfill **531 baris** = jumlah inquiry · nol durasi karangan |
| `20260828000002_inquiries_closure_fields` | **7 Sep 2026** | **6 kolom** penutupan + 2 FK + index · **5 trigger** total di `inquiries` · sidik-jari rantai WON identik sebelum-sesudah |

⚠️ **Header keempat file itu di branch masih menyatakan "PRODUKSI: belum dikonfirmasi" untuk sebagian di antaranya.** Yang berlaku adalah tabel ini. Koreksi header dikerjakan di branch secara terpisah.

**⏳ BELUM dijalankan — tersisa 5:**

| migrasi | status | catatan |
|---|---|---|
| `20260827000002_crm_v3_lifecycle` | ⛔ **TERTAHAN** | Me-RENAME `accounts.account_status` → `lifecycle_stage`; **produksi patah sebelum branch di-merge** (14 file / 29 baris hidup di `main` masih membaca kolom lama). Menunggu **Keputusan Terbuka #35** (jalur A vs B; **jalur B sudah dipilih, migrasi penggantinya belum ditulis**). ⚠️ Saat ditulis ulang: `set_prospect_on_inquiry` **disalin apa adanya**, daftar tahap tetap `('lead','mql','sql')` — lihat **Keputusan Terbuka #36**. |
| `20260830000003_inquiries_rls_owner_based` | siap, menyusul | Menukar RLS `created_by = auth.uid()` → `owner_id = auth.uid()`. **Prasyaratnya sudah lunas** (backfill `owner_id` 531/531, 6 Sep). Efek samping disengaja: `WITH CHECK` ikut berbasis `owner_id`, sehingga **pengoperan deal jadi aksi manager-ke-atas**. |
| `20260830000005_sales_targets` | siap, menyusul | Tabel target penjualan. **Nol pembaca di `src/` milik `main`** — aman kapan pun. |
| `20260830000001_crm_menu_permissions_sales` | siap, menyusul | Seeding menu permission role `sales`. |
| `20260830000004_accounts_source_add_whatsapp` | siap, **cek dulu** | Memperluas CHECK `accounts.source`. ⚠️ **Bandingkan CHECK barunya dengan nilai yang HIDUP di produksi sebelum dijalankan** (`cold_call`, `exhibition`, `existing_network`, `instagram`, `linkedin`, `other`, `referral`, `sales_visit`, `walk_in`, `website`, + NULL) — CHECK yang tak memuat salah satunya akan menolak baris lama. |

---


## Next Up

Berdasarkan kondisi LIVE (FASE 0-3 selesai) + `08_TECH_DEBT.md`:

**Storbit SP — lanjutan mesin status (bangun entitas baru, bukan wiring):**
1. ~~**FASE 4 — INVOICED** 📋 (belum dibangun): modul invoice baru — tabel invoice + line, penomoran (`increment_document_sequence`), relasi ke SP/BTB (`sp_order_id`/`sp_btb`), UI terbit invoice; gate = SP di TERKIRIM_PENUH/BTB_TERBIT. **Mulai dari AUDIT + DESAIN.**~~ **[8 Agu 2026 — SELESAI, KODE+UI+SQL SEMUA LIVE]** tabel `sp_invoices`/`sp_invoice_lines`/`sp_payments` + RPC `create_invoice`/`submit_invoice` + kartu Invoice `SalesOrderDetailPage.jsx` — dikonfirmasi live via `schema_snapshot.sql` refresh commit `8117127`, lihat "Selesai Terbaru (8 Agu 2026)" di atas. **Sisa langkah:** rekam migrasi (`supabase/migrations/`, belum ada file) → tes runtime end-to-end (belum sekali pun, gate `Σshipped=Σqty` belum pernah tercoba nyata dgn data produksi) → backfill historis `ar_ttfs.sp_order_id`/`invoice_id` (step M7, belum dikerjakan — kolomnya sendiri sudah live).
2. ~~**FASE 5 — LUNAS** 📋 (setelah FASE 4, TETAP belum dibangun sama sekali)~~ **[17 Agu 2026 — SELESAI, SQL+UI LIVE + tes end-to-end dilaporkan lolos]** `record_payment`/`mark_ttf_received` + jurnal AR minimal + cabang LUNAS di `sp_recompute_status` + UI pembayaran/TTF inline; lihat "Selesai Terbaru (16-17 Agu 2026)" di atas. **Sisa langkah FASE 4/5 yang MASIH terbuka:** backfill historis `ar_ttfs.sp_order_id`/`invoice_id` (step **M7**, belum dikerjakan — kolomnya live sejak 8 Agu) · TD-176 sisa 3 tabel · TD-178 hardcode SOA.
2a. **[17 Agu 2026, baru] Lanjutan Detail SP yang BELUM dibangun** (jangan dianggap selesai — sempat salah dilaporkan, sudah dikoreksi): 5 card grid Overview (Ringkasan Pesanan/Kontak Pelanggan/Catatan Internal/Dokumen Terkait/Aktivitas Terbaru) · instrumentasi `logAudit` di titik lifecycle SP (prasyarat tab History yang kini masih `EmptyState`) · badge Tersedia/Overdue per-item di tabel Items + sumber data kolom "Akun"/"Diskon" (belum ada kolomnya di skema) · tracking delivery telat outbound / SLA vendor accountability (baru investigasi, nol skema/RPC). Detail: `10_TASK_BREAKDOWN.md`.
2b. **[18 Agu 2026, baru] Dashboard Storbit — lanjutan.** Halaman intinya sudah LIVE; sisanya bukan bug melainkan cakupan yang sengaja belum digarap: isi `products.reorder_point` (menunggu SOP + belum ada UI edit ROP, Keputusan Terbuka #30) · konfirmasi klausa pinalti PKS (#28, kalau berubah ubah **dua** migrasi sekaligus) · aktifkan kotak "Cari nomor SP" (kini `disabled`) · beri pintu masuk drill-down untuk `terkirim_penuh` & `pernah_risiko_pinalti` (RPC-nya **sudah siap**, murni pekerjaan UI) · keputusan `delivery_notes.sp_order_id` (#29) · TD-200/TD-201/TD-202. Detail per-item: `10_TASK_BREAKDOWN.md` H8.
2c. **[21 Agu 2026] Partial picking — kode DAN DB sudah LIVE; sisa satu langkah: tes runtime.** Sisi FE tuntas (input qty per item, status `short` akhirnya terpakai, "Selesaikan Picking" tak bersyarat + dialog konfirmasi, prefill `qty_requested − qty_short` saat Mulai Pengambilan). ✅ **[KOREKSI 2 Sep 2026 — item ini sempat STALE dan menyesatkan.]** Teks lama menyatakan migrasi `20260821000001_partial-picking-guard.sql` "BELUM dijalankan" dan karenanya "perilaku produksi masih yang lama (satu picking per SP, permanen)". **Itu tidak benar sejak 21 Agu 2026.** Diverifikasi ulang langsung ke `supabase/schema_snapshot.sql` (bukan sekadar percaya salah satu dokumen): body `generate_picking_from_sp` yang LIVE **memuat guard baru** — blok `dn.dispatched_at IS NOT NULL` + pesan `'Picking list SP % sudah selesai tapi surat jalannya belum diberangkatkan…'` — persis isi migrasi itu. Header file migrasinya sendiri juga sudah bertanda `Status: LIVE … 21 Agustus 2026`, dan `CLAUDE.md` sudah memuat koreksi terverifikasi 24 Agu; hanya item roadmap ini yang tertinggal. Snapshot pun sudah lama ter-refresh. **Konsekuensinya: guard partial picking AKTIF di produksi** — TD-206 karena itu punya konsekuensi nyata, bukan teoretis. **Yang benar-benar tersisa: (1) tes runtime end-to-end** (nol tes browser sejauh ini), fokus pada jalur "selesaikan picking belum lengkap → SJ parsial → dispatch → generate picking kedua". Keputusan escape hatch untuk picking `done` buntu: TD-206 / Keputusan Terbuka #31.
2d. **[2 Sep 2026, baru] Detail SP — "paket kedua" redesain Edit Item: Item/Qty/Pricing/Tanggal jadi INLINE di tabel Items (bukan modal lagi).** Paket PERTAMA sudah selesai & LIVE 2 Sep 2026 (section "Finance & Dokumen" naik jadi kartu inline di tab Overview, ditulis lewat RPC `set_sp_finance_docs`; section "Finance & Dokumen" + "SP Information" dicabut total dari `EditItemModal`). Paket kedua **sengaja dipisah** — audit hari itu memetakan beberapa risiko HIGH yang berbahaya kalau digabung dalam satu langkah: **whole-row overwrite** (`update_sp_item_dual` menulis seluruh baris dari `jsonb_populate_record`, field yang tak dikirim jadi NULL), **`shipped_qty` bisa ter-rollback diam-diam** lewat jalur edit, dan **RLS `sp_items` masih longgar di 4 operasi**. **Kerjakan BERTAHAP per section, bukan sekaligus.** Peta lengkapnya ada di audit sesi 2 Sep 2026 (`PROGRESS.md` 2026-09-02 butir 2, "Temuan Bagian B"). Sampai itu digarap, Item/Qty/Pricing/Tanggal **tetap diedit lewat modal** — itu kondisi yang disengaja, bukan pekerjaan setengah jadi.
2e. **[5 Sep 2026, baru] Dashboard Storbit — sisa setelah Laporan Per Barang.** Tab 3 (Laporan Per Barang) + strip 4 kartu nilai + export Excel/PDF sudah LIVE. Yang **belum** dan sengaja tak digarap sesi itu: **(a)** **TD-217** — sampai `prospects_read` memuat role Finance, kolom **CUSTOMER kosong** di seluruh tabel Storbit untuk user Finance, jadi laporan ini setengah terbaca untuk audiens yang paling butuh; **(b)** **TD-218** — kartu Outstanding Piutang menampilkan **Rp 0** untuk user multi-entitas (home ≠ SOA); **(c)** sinkronisasi `sp_items` ↔ `sp_order_items` (gotcha #21 / TD-213) — hari ini divergensinya 0 baris jadi belum menggigit, tapi ia prasyarat sebelum angka "Outstanding Tagih" boleh dipakai sbg dasar keputusan penagihan; **(d)** membersihkan SP data uji **`ZZZTEST-DUEDATE-001`** yang ikut terhitung Rp 163.170 dari total piutang Rp 47.293.798 — kecil nominalnya, tapi ia **bocor ke laporan berorientasi CEO**; **(e)** tiga perbaikan kosmetik terakhir **belum ditest runtime**. Item 2b (18 Agu) tetap berlaku dan tak tergantikan oleh ini.


**Prioritas tech debt (detail: `08_TECH_DEBT.md`):**
3. **Enforce margin floor** (TD-38, HIGH) — quotation harus blok/warn bila margin < `margin_floor` (idealnya server-side di `save_quotation`); matriks diskon kini display-only.
4. **RLS hardening** (TD-39, HIGH) — perketat ~48 policy `USING(true)` (SP/gudang/dll) → company- + role-scoped (superset TD-04) + audit CRUD/DELETE (TD-03) + migrasi RBAC-driven (TD-01). **[7 Agu 2026] Cakupan & urutan eksekusi kini didetailkan penuh oleh TD-173** (audit RLS/RBAC menyeluruh — 19 tabel CRITICAL dgn breakdown severity per-tabel) — jadikan TD-173 rujukan utama saat item ini dikerjakan, JANGAN rencana terpisah. **[8 Agu 2026] +axis GRANT kolom-level (beda dari RLS baris-level di atas)** — `sp_orders`/`sp_invoices` **sudah ditambal & LIVE** (**TD-175 RESOLVED**); `sp_order_items`/`sp_items`/`sp_invoice_lines`/`sp_payments` masih terbuka (`GRANT ALL`, **TD-176** — pola fix TD-175 tinggal direplikasi persis).
4a. **Role Defaults Fase B3** (isi data) — tabel `role_menu_permissions` masih ~~KOSONG TOTAL~~ **[koreksi 9 Agu 2026: hampir kosong — sudah ada baris Picking List/Surat Jalan × 5 role dari migrasi `20260809000001`, itu scoped-fix bukan B3]**; halaman `RoleDefaultsPage` sudah bisa dipakai tapi belum ada default izin per role yang diisi. Belum dimulai. ✅ **[20 Agu 2026] SATU PRASYARAT SUDAH DIBERESKAN — baca ini sebelum mulai seeding.** Dua efek `App.jsx` memakai ekspresi `permsLoaded = role === 'super_admin' || userPermissions.length > 0 || menuPermissions.length > 0` yang **tak pernah true** untuk user yang aksesnya bersumber murni dari `role_menu_permissions` (persis kelompok yang B3 targetkan): `userPermissions` sudah mati, `menuPermissions` hanya memuat override per-user. Akibatnya redirect guard `activeMenu` + adopsi deep-link `?menu=` + penulis URL semuanya mati untuk kelompok itu. **Kalau B3 diseed tanpa fix ini, seeding justru MEMATAHKAN URL-sync & redirect guard** — makin banyak user dapat akses lewat role default, makin banyak yang kena. Sudah diganti `permissionsLoading || bnfAuthLoading` (sinyal yang sudah dipakai benar `canAccessActiveMenu`). ⚠️ **NOL tes runtime** atas fix itu — B3 idealnya dimulai dengan memverifikasi jalur ini dulu memakai SATU user non-super_admin ber-role-default. Detail: `04_ROLE_PERMISSION_MATRIX.md` §Peta Menu + `PROGRESS.md` 2026-08-20.
4b. **Fase C — satukan katalog menu.** 4 struktur menu hardcode di `App.jsx` (`CRM_MENU_ITEMS`/`ERP_MENU_GROUPS`/`NEXUS_NAV`/`MENU_KEY_MAP`, ~300 titik id-menu tersebar 1 file) belum disatukan dengan katalog DB (`modules`/`module_menus`/`menu_actions`/`module_actions` — saat ini cuma dipakai Permission Matrix, bukan sumber render menu utama). Belum dimulai, tak ada desain migrasi tertulis.
4c. **[9 Agu 2026, baru] Audit RLS pola `is_super_admin() OR get_user_company_id()` tanpa varian jamak** (TD-180, MEDIUM/HIGH) — sisir sistematis seluruh `CREATE POLICY` yang match pola ini tanpa `get_user_company_ids()`; mulai dari `payment_terms_read` (gap konkret sudah ketemu), prioritaskan tabel yang mungkin dibaca lintas-company oleh user non-admin. **[UPDATE 2 Sep 2026 — batch 2 & 3 selesai, kelasnya TETAP terbuka]** `sp_btb_read/insert/update` + `dc_master_read/insert/update` sudah ditambal (migrasi `20260902000006`/`20260902000007`, LIVE). **Sisir ulang pasca-refresh snapshot: 198 policy masih varian tunggal, tersebar di 76 tabel; `payment_terms_*` tetap OPEN.** ⚠️ **Naikkan prioritasnya:** dua kali berturut-turut (18 Agu, 2 Sep) kelas bug ini ditemukan lewat **keluhan user**, bukan audit — karena gagalnya senyap (nol baris / embed NULL, **bukan error**).
4d. **[9 Agu 2026, baru] Bereskan orphan pasca-AdminHub** (TD-181, LOW) — tambah `bulk-edit-price`/`bnf-org-roles`/`schema-manager` ke daftar redirect legacy `App.jsx`, lalu hapus 3 blok render lama + `AdminShell.jsx`/`AdminSettingsHub.jsx`(+9 file) setelah dikonfirmasi aman (pola TD-41/`AppLauncher.jsx`).
5. **Drop `sp_btbs` + dead code cleanup** (TD-41) — 4 helper legacy `db.js` (0 caller) + tabel `sp_btbs` (data migrasi) + `AppLauncher.jsx`.
6. Sisanya (TD-40 2D sync — **[8 Agu 2026] RESOLVED & live, lihat TD-40 di `08_TECH_DEBT.md`** · TD-42 rank doc/`DESIGN_SP_SCHEMA` · TD-43 integrasi email/n8n · TD-44 EF docs · TD-177 test regresi visual PDF · TD-178 hardcode SOA `sp_recompute_status`+`dispatch_delivery`) → rujuk `08_TECH_DEBT.md`.

**Backlog domain lain (open):**
- **Procurement PRF lanjutan** — Fase 2 (child fields Sea/Air/Inland/Project/Custom di form) **DONE 10 Jul**; sisa: Fase 3a (list/inbox procurement + acknowledge), Fase 3b (cross-entity inbox). Rujukan: `AUDIT_PROCUREMENT.md`. **⭐ [27 Jul 2026] fondasi DB untuk sebagian Fase 3a sudah ADA** (tabel `prf_vendor_offers` + RPC `prf_claim`/`prf_release`/`prf_mark_quoted`/`prf_select_offer` — model award vendor dibalik, sales yang memilih) ~~**TAPI UI-nya nol** — form/list/panel pilih penawaran belum dibangun sama sekali; ini BUKAN Fase 3a selesai, hanya fondasinya.~~ **[KOREKSI 27 Jul 2026 sore — SEBAGIAN USANG, "Batch 3A", `feat/prf-audit`, belum di-commit]** Bagian **"acknowledge"** yang disebut definisi Fase 3a di kalimat pertama baris ini **kini ADA** — tombol "Ambil PRF Ini"/"Lepas PRF" (`prf_claim`/`prf_release`) di `PRFDetailPage.jsx`, plus kartu baca `prf_vendor_offers` (SELECT) read-only. **TAPI ini BUKAN Fase 3a selesai** — bagian **"list/inbox"** (mis. memfilter list "Forwarding (MSI)" per siapa yang sedang memegang PRF) **TIDAK ikut dibangun**; ~~form input penawaran, panel pilih penawaran, `prf_mark_quoted`/`prf_select_offer` MASIH nol UI.~~ **[KOREKSI 27 Jul 2026, sesi terpisah SETELAH Batch 3A — Batch 3B, 3 file FE `feat/prf-audit` belum di-commit]** form input **penawaran (tambah/edit/hapus, `PRFVendorOfferModal.jsx`) SUDAH ADA** — gate `canManageOffers` (HANYA pemegang PRF sendiri, TANPA fallback manager, beda dari `canRelease`); ~~**panel pilih penawaran untuk SALES (3C)** dan **`prf_mark_quoted`/`prf_select_offer`** **TETAP** genuinely nol UI.~~ **[KOREKSI 27 Jul 2026, sesi terpisah SETELAH Batch 3B — Batch 3C, 4 file FE `feat/prf-audit` belum di-commit]** **Fase 3a kini PRAKTIS TUNTAS dari sisi kapabilitas** (list/inbox filter-per-pemegang masih belum, tapi acknowledge+quote+select semua sudah ada): tombol "Nyatakan Penawaran Siap" (`prf_mark_quoted`, procurement) + tombol "Pakai/Ganti Penawaran Ini" (`prf_select_offer`, `DealDetailPage.jsx`, sales — panel pilih penawaran yang dulu disebut "3C" **kini ADA**). **TD-156 kini RESOLVED (kode)** — dasar hitung modal "Buat Quotation" pindah dari `is_awarded` murni ke UNION (baris warisan `offer_id IS NULL` tetap `is_awarded=true`, baris `offer_id=selected_offer_id` tanpa syarat `is_awarded` sama sekali — detail nuansa: `08_TECH_DEBT` TD-156). Juga BELUM ada: notifikasi PRF; transisi `CANCELLED`/`EXPIRED` (TD-154, TAK BERUBAH). Detail: `PROGRESS.md` 2026-07-27 (entri Batch 3A + Batch 3B + Batch 3C) + `03_DATA_MODEL` entri `prf_vendor_offers` + `05_WORKFLOW_MAP` §Procurement — PRF (subbagian "Penawaran Vendor") + `08_TECH_DEBT` TD-156.
- **CRM/Quotation gates** — verifikasi enforcement approval diskon/margin (downstream?) + BANT gate (`05_WORKFLOW_MAP.md` — Gate & Approval).
- **RBAC/RLS `accounts`** + dropdown role-scope (TD-01/04/06).
- **Runtime-verify staging** (accounts/Activity cutover) + **deploy Edge Functions** (TD-21/22) + **drop dormant** `sales_calls`/`visits`/`customers`/`profiles.role` (TD-18/19/20).
- **Modul Finance** transaksi umum (Billing/AR-AP) — arah setelah foundation matang. **Audit logging** (TD-05) ✅ done.
- **Inventory — perencanaan alokasi barang ke klien** 📋 (belum ada; `reserved`/`available` di stok = reservasi SP fulfillment, BUKAN perencanaan alokasi). (dari 14_BACKLOG_RECON.md D19)
- **Inventory — stock monitoring: aging & turnover** 📋 (belum ada; Inventory Dashboard punya movement-trend & low-stock, tapi **nol** KPI aging/turnover — terverifikasi `grep aging|turnover` inventory = 0; `stock_ledger` bisa jadi basis data). (dari 14_BACKLOG_RECON.md D20)
- **Inventory — Stock Opname (Full Count/Cycle Count)** 📋 (belum ada; desain skema lengkap 8/8 keputusan terkunci — 4 tabel baru `warehouse_locations`/`stock_count_sessions`/`stock_count_items`/`stock_count_variance_reports` + RLS/RPC dimodel dari pola live `dc_master`/`sp_orders` — belum satu baris pun dieksekusi ke DB). Lihat `DESIGN_STOCK_OPNAME_SCHEMA.md`.
- **CRM — upload MOU ke Nexus** 📋 (belum ada; **nol `storage.upload` di CRM**, `deal_handovers.msa_status` hanya text; butuh desain Supabase Storage + kolom/tabel MOU). Ini juga rumah untuk item `18` C-13. (dari 14_BACKLOG_RECON.md C16)
- **CRM — Aktivitas tarik status MOU/TOP** 📋 (belum ada; bergantung MOU di atas; data TOP sudah ada di `top_requests`). (dari 14_BACKLOG_RECON.md C17)

**Near-term:** dashboard Indomarco — halaman `IndomarcoDashboardPage` sudah **LIVE**; polish/iterasi sesuai kebutuhan presentasi.

**Near-term — UAT modul BNF/Briefing Harian/Meeting Mingguan:** dokumen sign-off UAT (FRM-IT-11) sudah diproduksi 5 versi (1 gabungan + 2 khusus staff [scope Briefing Harian saja] + 2 khusus manager [scope Briefing Harian+BNF+Meeting Mingguan]), dikirim 12 Agu 2026 utk memulai eksekusi UAT — bahan kerja rapat koordinasi kepala departemen 13 Agu 2026. Fakta proses, bukan fakta kode — lokasi file FRM-IT-11 belum dikonfirmasi ada di repo. Lihat `PROGRESS.md` 2026-08-12.

**Prioritas tech debt — BNF/Briefing Harian/Meeting Mingguan (detail: `08_TECH_DEBT.md` TD-184…TD-194):**
8. **TD-184 (MEDIUM)** — rekonsiliasi `bnf_divisions`/`bnf_departments` ke SK org chart resmi (SK 002/OD/HCGA-MSI/V/2026); banyak dept ditriplikasi flat padahal group-wide, banyak posisi resmi belum ada barisnya. Butuh audit + redesain, bukan tambal cepat.
9. **TD-193 (MEDIUM, kehati-hatian)** — profil "Deny Triawan" berrole `super_admin` tapi fungsi Manager Logistik; audit dampak lintas-modul DULU sebelum role diubah (super_admin bypass RLS luas).
10. Sisa TD-185…TD-192/TD-194 (LOW) — gap notifikasi (downgrade eskalasi, cron reminder, Irisan departemen, reassign PIC), auto-pull Aktivitas dari modul lain, dan 1 flag data-quality (email domain Ayun Ngainurrohmah) — kerjakan oportunistik.

7. [TODO: konfirmasi prioritas bisnis berikutnya dengan product owner].

---

## Keputusan Terbuka (perlu keputusan Den)

> Peta keputusan produk terpusat — hal yang **belum diputuskan** (bukan fakta/selesai). Dari rekonsiliasi backlog. Section ini akan diperluas di sub-fase berikutnya (append-friendly).

1. **Dashboard boleh dibagi ke customer eksternal?** — `IndomarcoDashboardPage` sekarang ditandai INTERNAL (framing customer-facing tapi halaman internal). **Perlu keputusan Den:** boleh dibagikan ke customer eksternal? tujuan & batasannya? (dari 14_BACKLOG_RECON.md A1)
2. **Intake SP dari Indomarco — automasi email vs API.** — Email SP kadang tak diterima MSI. **Perlu keputusan Den:** solusi di sisi MSI (automasi email konfirmasi) atau integrasi API ke Indomarco? (butuh keputusan + desain integrasi; kerabat TD-43 verifikasi email). (dari 14_BACKLOG_RECON.md A2)
3. **Kontrak 20 DC vs aktual 44 DC — akun Nexus per DC?** — `dc_master` sudah memuat 45 DC (36 Indomarco ter-mapping) sebagai DATA, bukan keputusan. **Perlu keputusan Den:** buat akun/penomoran Nexus per DC atau tidak? (dari 14_BACKLOG_RECON.md A3)
4. **Hilangkan approval Lead Pool — cukup justifikasi wajib?** — Sekarang: pull Lead Pool butuh justifikasi ≥20 char + approval manager (`LeadPoolApprovalPage`). **Perlu keputusan Den:** buang tahap approval, cukup remarks/justifikasi WAJIB? (kerabat TD-77 celah RLS `pull_status`). (dari 14_BACKLOG_RECON.md C12)
5. **"Aktifkan modul Shipment untuk mengaktifkan form Handover" (Koh Deny) — maksud ambigu.** — Handover form SUDAH ter-wire ke WON (gate nilai Rp100jt) & `ShipmentPage` sudah ada. **Perlu keputusan Den / klarifikasi:** apa maksud "Shipment mengaktifkan Handover"? relasinya belum terdokumentasi. (dari 14_BACKLOG_RECON.md C14)
6. **Sinyal "lunas" untuk kunci harga/kategori SP (H7 ④).** — Guard beku sudah ada di `sp_recompute_status`, tapi belum ada mekanisme yang MENYETEL status lunas. **[8 Agu 2026]** FASE 4 (INVOICED/SUBMITTED) kini kode-siap (SQL belum live) — TAK menjawab pertanyaan ini, LUNAS tetap murni FASE 5 (belum dibangun sama sekali). **Perlu keputusan Den:** sinyal mana jadi acuan — (a) `sp_orders.status='LUNAS'` via FASE 5, (b) `ar_ttfs.tgl_pembayaran` (domain AR), atau (c) kolom status pelunasan baru diisi manual? (c bentrok dgn FASE 5.) **[17 Agu 2026 — PRAKTIS TERJAWAB OLEH IMPLEMENTASI, tapi belum ada keputusan eksplisit tertulis]** FASE 5 kini live dan menempuh jalur **(a)**: `sp_orders.status` naik ke `LUNAS` dari fakta `sp_invoices.status='paid'` (yang sendiri diturunkan dari Σ`sp_payments`), lalu **dibekukan** guard `IN ('CANCELLED','LUNAS')`. **Tetap dibiarkan terbuka** karena pertanyaan aslinya soal *kunci harga/kategori SP* (H7 ④) — perlu konfirmasi Den apakah konsumen guard itu memang boleh membaca sinyal (a) ini, dan apa nasib `ar_ttfs.tgl_pembayaran` yang masih hidup sbg field terpisah. (detail: `10_TASK_BREAKDOWN` H7 ④ · dari 18 S-04④)
7. **Domain nilai `sp_items.price_category` belum baku.** — `sp_order_items.price_category` ber-CHECK ketat (`semester/tahunan/project`); `sp_items.price_category` tanpa CHECK (dipakai `default`/`semester`/`legacy`). **Perlu keputusan Den:** satukan domain (beri CHECK di `sp_items`) atau biarkan longgar? Backfill menunggu ini. (detail: TD-72 · dari 18 S-05)
8. **Quotation ACCEPTED — bikin tombol atau buang trigger.** — Nol penulis `ACCEPTED` di kode → 88% deal WON nilainya kosong; trigger `sync_deal_value_on_quotation_accept` live tapi tak pernah jalan. **Perlu keputusan Den:** tambah aksi "Terima" (SENT→ACCEPTED) atau nyatakan ACCEPTED mati & buang trigger? (detail: TD-54 · dari 18 C-01)
9. **NURTURE = lubang hitam.** — ~~6 akun **bisa di-set** NURTURE dari form~~ **[USANG 22 Jul 2026]** opsi `NURTURE` **sudah DICABUT** dari form Prospect → **tak ada baris NURTURE baru**; keenam baris warisan juga **tak lagi bisa tertimpa diam-diam** lewat Edit Deal (penjaga `isKnownStage`). **[Diperbarui lagi 22 Jul 2026 — batch 3B-1]** const lokal `PIPELINE_STAGES` itu kini **dihapus seluruhnya**; dropdown form Prospect diturunkan dari `ACTIVE_STAGE_KEYS` (`DealPanels.jsx`) = **3 nilai**, dan **tak ada satu pun jalur FE** yang bisa menulis nilai stage di luar NEW/CONTACTED/QUALIFIED. Nilai warisan **tetap DIRENDER** (daftar render masih 7). **Yang MASIH terbuka:** keenam baris itu **tak ada di Kanban & tak ada aturan aging** → tetap hilang dari pipeline. **Perlu keputusan Den:** beri kolom Kanban / aturan aging / atau migrasikan ke Lead Pool? *(rencana Fase 3 menurut brief Den = migrasi ke Lead Pool bersama backfill `pipeline_stage`; **belum dijalankan** — konfirmasi sebelum dieksekusi.)* (detail: TD-61 · dari 18 C-02 · `AUDIT_FASE3_20260722.md` H1)
10. **LOST tak punya siklus hidup.** — Hanya 7/997 akun LOST; lead mati menumpuk di stage aktif. **Perlu keputusan Den (butuh desain):** LOST masuk pool atau tempat sendiri? reaktivasi lewat mana? (detail: TD-58 · dari 18 C-10)
11. **`notification_rules` yatim.** — 11 rule aktif, semua MSI, nol pembaca (notif ditulis langsung tanpa lewat rules). **Perlu keputusan Den:** bangun rule engine atau buang tabel? (detail: TD-53 · dari 18 C-15)
12. **Costing / gate margin PRF — dibangun atau manual?** — PRICING_LANE tereduksi jadi form input tanpa costing/approval; harga quotation diketik tangan. **Perlu keputusan Den:** modul costing dibangun, atau proses ini manual di luar sistem? (detail: TD-83 · dari 18 P-04)
13. **Sign-off handover & TOP Request — manual di luar sistem atau belum dibangun?** — Kolom `approved_by_*` + status TOP tak pernah dimajukan; teks "dual sign-off" = copy UI. **Perlu keputusan Den:** approval manual di luar sistem (→ hapus teks menyesatkan) atau bangun alurnya? (detail: TD-78 · dari 18 C-05)
14. **Custom PRF butuh 2 syarat — membingungkan?** — Blok Custom muncul hanya bila `service_type='custom'` DAN add-on Custom Clearance. **Perlu keputusan Den (nunggu user testing):** pertahankan atau sederhanakan? (detail: TD-84 · dari 18 P-06)
15. **Fase 3b — inbox PRF cross-entity.** — Butuh cabang RLS custom role `procurement` lintas-3-entitas; bergantung inbox single-entity dulu. **Perlu keputusan Den + desain RLS:** kapan & bagaimana? (detail: TD-85, TD-76 · dari 18 P-07)
16. **Selisih data import SP belum diklarifikasi.** — "Selisih SP 431 vs 435 (Gigih)" + "mapping 30 item kontrak PKS Indomarco" (di `CLAUDE.md` Current Phase, bertanda PERLU KONFIRMASI). **Perlu keputusan Den / klarifikasi Gigih & Indomarco:** angka dasar SP belum disepakati. (belum ada TD — data clarification · dari 18 S-13)
17. ~~**Gate handover Rp100jt — masih aturan bisnis yang berlaku?**~~ **✅ DIJAWAB 22 Jul 2026 — BUKAN LAGI TERBUKA.** **Keputusan Den: aturan bisnisnya TETAP BERLAKU.** Implementasinya **sengaja dibiarkan nganggur** sampai **papan Deal per-inquiry (F4-6)** dibangun; di situ gate Light/Strategic akan membaca **`inquiries.estimated_value`** (kolom yang lahir di F3-2), bukan `accounts.estimated_value`. **`LightHandoverModal`/`StrategicHandoverModal` JANGAN dihapus & JANGAN dicatat sbg TD dead-code** — **tunggakan berjadwal**, bukan sampah. (detail: `05_WORKFLOW_MAP` §CRM Gate & Approval + `03_DATA_MODEL` `deal_handovers`)
18. ~~**Konversi otomatis "deal menang → customer" mati sementara?**~~ **✅ DIJAWAB 22 Jul 2026 — PREMISNYA KELIRU.** Konversi **tidak mati, pemicunya PINDAH**, dan itu memang **tujuan** batch 3B-1: **SO jadi `SENT` → `trg_inquiry_won` → `inquiries.status='WON'` → `trg_set_customer_on_inquiry_won` (`20260722000008`) → `accounts.account_status='customer'`** + penerbitan kode customer. Trigger lama `set_customer_on_won` **tetap hidup berdampingan** sampai dicabut di **F3-8 (batch 3C)**. *Rantai ini diverifikasi Den 22 Jul 2026 (bukan verifikasi doc-keeper).* (detail: `05_WORKFLOW_MAP` §CRM "Rantai konversi customer" + `08_TECH_DEBT` TD-94)
19. ~~**Aksi "Tandai Kalah" tidak menulis `audit_logs` — disengaja?**~~ **✅ DIJAWAB & SUDAH DIKERJAKAN 22 Jul 2026.** Den: *"harus berjejak, alasannya dipakai menghitung win rate."* `markInquiryLost` kini menulis `audit_logs` (`UPDATE_INQUIRY`/`INQUIRY`, `entityLabel` = `inquiry_no`, `notes` = `"{status lama} → LOST · alasan: {lost_reason}"`), fire-and-forget seperti `saveDealUpdate`; `auditLogger.js` tak disentuh.
20. ~~**"Satu perusahaan = satu akun" — cakupan PER-ENTITAS, hard-block.**~~ **✅ DIJAWAB & SUDAH DIEKSEKUSI 25 Jul 2026 — BUKAN LAGI TERBUKA.** **Hard-block-nya AKTIF di level tabel:** UNIQUE index partial **`uq_accounts_norm_name_per_entitas`** atas (`company_id`, `normalize_account_name(name)`) `WHERE deleted_at IS NULL`, memakai fungsi **`normalize_account_name(text)`** (IMMUTABLE — buang PT/CV/TBK + non-alfanumerik + lowercase) + extension **`pg_trgm`**; dipasang **manual di DB**. Menyusul di atasnya: **lapisan UX** (25 Jul, FE-only 3 file) — pre-check **fuzzy non-blocking** lewat RPC `check_similar_accounts` + **handler `23505` manusiawi** di ketiga jalur create. **⚠️ TIGA CATATAN YANG JANGAN HILANG:** **(a)** RPC `check_similar_accounts` kini **berjejak `20260725000004`** + **sudah dijalankan DAN TESTED**: `SELECT check_similar_accounts('ALLIANCE COSMETIC', <uuid MSI>)` balik 3 baris similarity 1.0/0.842/0.619; ~~lapisan fuzzy-nya **belum tes runtime** (form di balik login)~~ **[KOREKSI SUSULAN 25 Jul 2026]** smoke test UI membuktikan warning fuzzy **muncul** di jalur Prospect **dan** Customer CRM — TERBUKTI JALAN; **hard-block DB tetap penegak**. **(b)** ✅ **KINI BERJEJAK MIGRASI (KOREKSI 25 Jul atas "NOL jejak di repo"):** merge duplikat (`20260725000001`/`000002`) · `normalize_account_name` + index + `pg_trgm` (`20260725000003`) · RPC (`20260725000004`) → **TD-128 RESOLVED (jejak)**. **⚠️ Tapi snapshot BELUM di-`pg_dump`** → masih **menyesatkan** untuk `accounts` sampai refresh (**"berjejak migrasi" ≠ "ada di snapshot"**); + drop 6 backup lama (lihat `03_DATA_MODEL` gotcha #10 + **TD-128**). **(c)** Prasyarat yang dulu dicatat di sini — **audit dedup lanjutan + merge manual data kotor** — **sudah dijalankan** (7 grup di-merge) sebelum index dipasang; duplikat yang dulu didaftar (TERANG DARMA PERKASA · ANDI · CHEMCO HARAPAN NUSANTARA vs CHEMCO NUSANTARA · KEENAM FREIGHT FWD vs KEENAM INTERNATIONAL AIR TRANSPORT) tak lagi jadi penghalang. *(Bahwa objek-objek itu live di DB = **pernyataan Den**; yang doc-keeper verifikasi = ketiadaannya di repo.)* **Arsip pertimbangan sebelum eksekusi (tetap berguna kalau aturannya ditinjau ulang):** *(Label Den di sesi 24 Jul: **"K-3"**.)* **Isi keputusan:** duplikat nama perusahaan **diblokir keras** (hard-block, bukan warning lunak), dan **cakupan pengecekannya PER-ENTITAS** — bukan global. **Prasyarat sebelum boleh dieksekusi (jangan dilompati):** **(1)** audit dedup **lanjutan** + **(2)** merge manual data kotor lebih dulu — memasang constraint di atas data yang masih kembar akan **menolak baris sah** dan/atau menggagalkan migrasi. **Duplikat yang sudah teridentifikasi sejauh ini (dari Den, ~~belum dituntaskan~~ **[SUDAH di-merge sebelum index dipasang, 25 Jul]**):** TERANG DARMA PERKASA · ANDI · CHEMCO HARAPAN NUSANTARA vs CHEMCO NUSANTARA · KEENAM FREIGHT FWD vs KEENAM INTERNATIONAL AIR TRANSPORT. **Konteks pendukung:** `AUDIT_DEDUP.md` (audit read-only 24 Jul; ~~root repo, belum diarsip~~ **[KOREKSI 25 Jul: sudah diarsipkan ke `docs/archive/audits/`]** — lihat **TD-126**). ⚠️ **Dua hal dari audit itu yang wajib diputuskan DULU, jangan diasumsikan:** **(a)** kolom entitas mana yang jadi acuan — `company_id` (NOT NULL, dipakai RLS) atau `owner_company_id` (**NULLABLE**, dipakai filter entitas UI + penerbitan kode customer) **[TERJAWAB 25 Jul: index memakai `company_id`]**; keduanya diisi sama saat INSERT tapi **tak ada constraint yang memaksa tetap sama**; **(b)** dedup massal 22 Jul (`20260722000005`) dijalankan **GLOBAL tanpa scoping entitas** dan dengan normalisasi **lebih agresif** (membuang PT/CV/TBK + non-alfanumerik) — jadi **angka sisa duplikat dari pass itu BUKAN jawaban** untuk aturan per-entitas ini. Selain itu ada **tiga jalur INSERT `accounts`** dengan **tiga perilaku dup-check berbeda dan tak satu pun memblokir** (`ProspectFormPage.jsx:267` scope `company_id`+pra-customer · `CustomerListPage.jsx:366` **tanpa scope entitas** · `db.js:264` lewat `CustomerModal` — satu-satunya hard-block, tapi client-side terhadap list in-memory) → **ketiganya harus diseragamkan**, kalau tidak constraint DB akan memunculkan error mentah `23505` di jalur yang belum disiapkan (preseden penanganan rapi ada di `VendorListPage.jsx:202-207`). *(Atribusi: isi keputusan + daftar duplikat = pernyataan Den; butir (a)/(b)/jalur INSERT = temuan `AUDIT_DEDUP.md`, doc-keeper tidak menjalankan query apa pun.)* Dicatat 24 Jul 2026.
21. **PRF/001 & PRF/005 — status `SUBMITTED` tapi `selected_offer_id` sudah terisi (data legacy dari backfill "Penawaran Vendor", kombinasi yang tak bisa lahir dari RPC).** Migrasi `20260727000002_prf_vendor_offers_backfill.sql` menerjemahkan model lama (`prf_cost_items.is_awarded`) ke model baru (`prf.selected_offer_id`) TANPA menyaring status PRF sama sekali — akibatnya (klaim Den, belum diverifikasi query DB oleh doc-keeper) dua PRF punya kombinasi status+`selected_offer_id` yang mustahil lahir dari RPC `prf_select_offer` (yang mensyaratkan status `QUOTED` sebelum mengisi kolom itu, RAISE kalau tidak — diverifikasi kode `20260727000003_prf_rpc_transisi.sql:162-164`). **Perlu keputusan Den:** naikkan status kedua PRF ini ke `QUOTED` secara manual, atau biarkan sebagai data legacy yang dikecualikan dari asumsi model baru? (dari sesi doc-keeper 27 Jul 2026, modul "Penawaran Vendor" · `03_DATA_MODEL` entri `prf_vendor_offers` · `05_WORKFLOW_MAP` §Procurement — PRF)
22. **RLS `accounts` (`prospects_read`) — cakupan `procurement`/`operations` kemungkinan over-broad, sengaja BELUM diperbaiki.** Temuan audit RLS/RBAC 7 Agu 2026 (**TD-173**, kategori HIGH): kedua role ini kemungkinan bisa lihat SEMUA account (bukan cuma yang assigned ke mereka), berbeda dari `sales` yang scoped ke `assigned_to`/`created_by`. **Perlu keputusan Den:** apakah `procurement`/`operations` MEMANG seharusnya lihat semua account se-company (mis. utk keperluan cross-check PRF/SP), atau ini kelebihan-lebar yang tak disengaja dan perlu diperketat ke pola `sales`? **JANGAN diperbaiki sepihak/asumsi status ini salah** — tunggu jawaban. (dari audit RLS/RBAC 7 Agu 2026 · `08_TECH_DEBT.md` TD-173)
23. **Kandidat duplikat akun baru — verifikasi manual, DAN pertanyaan terbuka soal daftar "~18 kandidat fuzzy dari K-3" yang dirujuk tapi tak ketemu.** Dua pasangan baru dari sesi 7 Agu 2026: **"MESIN KAFE & SERVICES" vs "MESIN KAFE DAN SERVICE"**, **"CHEMCO NUSANTARA" vs "CHEMCO HARAPAN NUSANTARA"**. **⚠️ Instruksi aslinya minta pasangan ini ditambahkan ke daftar "~18 kandidat fuzzy dari K-3 sebelumnya" yang sudah ada — doc-keeper MENCARI daftar itu (di sini/item 20, `PROGRESS.md`, `docs/archive/audits/AUDIT_DEDUP.md`) dan TIDAK MENEMUKANNYA** — `AUDIT_DEDUP.md` §Q3b cuma berisi query SQL TEMPLATE (tanpa hasil tersimpan), dan tak ada angka "18" dgn daftar pasangan tersimpan di dokumen mana pun yang doc-keeper bisa akses. Kemungkinan besar daftar itu, sama seperti audit RLS/RBAC di atas, cuma pernah ada di riwayat chat sesi lain yang tak tertulis ke file. **Dicatat di sini sbg lokasi terdekat yang relevan (item 20 = keputusan dedup K-3), BUKAN sebagai kepastian ini "daftar yang sama".** ⚠️ **Catatan tambahan yang perlu dikonfirmasi Den:** pasangan **"CHEMCO NUSANTARA" vs "CHEMCO HARAPAN NUSANTARA"** SUDAH tercatat sebagai salah satu duplikat yang **di-merge 25 Jul 2026** (lihat item 20, "Duplikat yang sudah teridentifikasi sejauh ini ... [SUDAH di-merge sebelum index dipasang, 25 Jul]") — kalau pasangan ini muncul lagi sbg kandidat 7 Agu 2026, kemungkinan **(a)** merge lama tak sepenuhnya menutup semua baris (mis. baris baru masuk lagi setelahnya), atau **(b)** ini nama yang mirip tapi bukan akun yang sama persis. **Perlu verifikasi Den sebelum diasumsikan salah satunya.**
24. **QR code di InvoicePDF — belum diputuskan mau mengarah ke mana.** Placeholder kotak putus-putus kosong dipasang sengaja di layout (`InvoicePDF.jsx`, sesi 8 Agu 2026) — posisi/ukuran sudah sesuai desain sumber (Claude Design "Storbit commercial invoice"), tapi ISI-nya (link verifikasi? link pembayaran? sesuatu yang lain?) **belum diputuskan Den**. **Perlu keputusan Den:** QR ini mengarah ke mana, dan kapan dikerjakan (follow-up terpisah, bukan bagian FASE 4 ini)? (dari sesi Invoice FASE 4, 8 Agu 2026 · `03_DATA_MODEL.md` entri `sp_invoices`)
25. **[17 Agu 2026, FASE 5] Lokasi penyimpanan bukti potong PPh 23 FISIK belum ditentukan.** Sistem hari ini cuma menyimpan **URL manual** (`sp_payments.bukti_potong_url`, COMMENT-nya sendiri menandai "interim") + nomor bukti potong — tak ada upload terintegrasi ke Supabase Storage. **Perlu keputusan Den + tim finance:** dokumen fisik/scan disimpan di mana (Drive folder tertentu? Storage bucket Nexus?), siapa yang bertanggung jawab menaruhnya, dan apakah upload mau dibangun di Nexus. Sampai itu diputuskan, jangan bangun uploader — bentuk penyimpanannya bisa berubah. (dari sesi doc-keeper 17 Agu 2026 · `03_DATA_MODEL.md` §`sp_payments`)
26. **[17 Agu 2026, FASE 5] Badge "Issued" + "Lunas" tampil BERSAMAAN di card Invoice — disengaja atau bug?** Dikonfirmasi doc-keeper langsung dari kode (`SalesOrderDetailPage.jsx`): badge utama cuma bercabang dua (`status==='submitted'` → "Submitted", **selain itu** → "Issued", termasuk saat status sudah `partial`/`paid`), sementara badge "Lunas" adalah blok TERPISAH yang muncul saat `status==='paid'`. Jadi invoice lunas menampilkan "Issued" dan "Lunas" berdampingan. **Perlu keputusan Den:** badge utama diperluas mengenali `partial`/`paid` (mis. "Sebagian Dibayar"/"Lunas" menggantikan "Issued"), atau memang mau tetap dua badge (satu tahap dokumen, satu tahap pembayaran)? Kosmetik, tak menyentuh data. (dari sesi doc-keeper 17 Agu 2026)
27. **[17 Agu 2026, FASE 5] Dashboard "Outstanding Finance" lama belum terhubung ke sistem invoice/payment baru.** Angka outstanding lama bersumber dari domain AR Tracker (`ar_ttfs`/`ar_btbs`), sedangkan kebenaran uang kini di `sp_invoices`/`sp_payments` (+jurnal AR). **Perlu keputusan Den:** dashboard dipindah membaca sumber baru, dibiarkan jalan paralel selama masa transisi, atau dipensiunkan? Terkait erat dgn nasib domain `ar_btbs` (lihat `08_TECH_DEBT.md` **TD-197**). (dari sesi doc-keeper 17 Agu 2026)
28. **[18 Agu 2026] Klausa pinalti di PKS: `dispatched_at` atau `delivered_at`?** Metrik `pernah_risiko_pinalti` sekarang memakai **`dispatched_at`** (berangkat dari gudang) — dipilih karena datanya lebih andal: distempel pada aksi gudang nyata sekalian menulis `stock_ledger` outbound, sementara `delivered_at` distempel `mark_delivery_delivered` yang bisa telat berhari-hari → itu mengukur kedisiplinan input, bukan ketepatan kirim. **Yang menentukan bukan preferensi teknis melainkan bunyi klausa di PKS.** Kalau pinalti dihitung dari TANGGAL TERIMA customer, ganti ke `delivered_at` — **perubahan 1 baris** tapi di **dua** migrasi (`20260818000002` + `20260818000003`, klausanya kembar) — dan angka sekarang harus dianggap **terlalu optimis**, karena pengiriman yang berangkat tepat waktu tapi SAMPAI telat tidak terdeteksi sama sekali.
29. **[18 Agu 2026] Backfill `delivery_notes.sp_order_id` — dikerjakan, atau kolomnya dipensiunkan?** Kolom itu di-backfill sekali 6 Jul 2026 lalu tak pernah diisi lagi (`generate_delivery_from_picking` tak menyertakannya di INSERT), jadi **semua surat jalan baru ber-`sp_order_id` NULL** dan proporsinya hanya bertambah. Semua join hari ini (termasuk RPC dashboard & `sp_recompute_status`) memakai komposit `(customer_id, sp_no)`. Dua jalan: isi kolomnya di RPC + backfill sekali, **atau** akui komposit sebagai kunci resmi lalu drop kolomnya.
30. **[18 Agu 2026] Angka `products.reorder_point` menunggu SOP Storbit.** Kolomnya sudah live tapi **nol baris terisi** by design — angkanya ditentukan Finance/Warehouse Controller, bukan sistem. Sampai diisi, kartu "Danger Stock" selalu 0 dan "ROP Belum Diisi" = seluruh produk aktif. Perlu keputusan **siapa yang mengisi dan lewat layar mana** — belum ada UI untuk mengedit ROP sama sekali.
31. **[21 Agu 2026] Escape hatch untuk picking `done` yang buntu — bentuknya apa?** Sejak guard partial picking, picking berstatus `done` memblokir picking berikutnya sampai surat jalannya dibuat DAN diberangkatkan, sementara `cancel_picking` menolak status `done` → SP itu tak punya jalan keluar lewat UI. **Blokirnya sendiri sudah diputuskan BENAR** (Opsi B, keputusan sadar Den: reservasi stoknya nyata masih tertahan) — yang belum diputuskan adalah pintu keluarnya: melonggarkan `cancel_picking` untuk picking `done` yang belum punya SJ, membuat aksi "batalkan sisa", atau membiarkan (menerima bahwa kasus ini ditangani manual lewat DB). Ikut diputuskan: nasib flag permanen `sp_orders.had_cancelled_picking` untuk kasus ini. Analisis lengkap + sub-kasus paling keras (semua item `qty_picked=0` → SJ-nya bahkan tak bisa dibuat): `08_TECH_DEBT.md` **TD-206**. ~~**Tidak mendesak selama SQL guard-nya belum dijalankan.**~~ **[KOREKSI 24 Agu 2026: guard SUDAH LIVE sejak 21 Agu — alasan "tidak mendesak" itu GUGUR. Pertanyaannya kini aktif di produksi.]** **[pembaruan hari yang sama — satu opsi sudah dieksekusi, pertanyaannya BELUM tertutup]** Den memilih **"blokir di UI"**: `PickingListDetailPage.jsx` kini menolak "Selesaikan Picking" selama seluruh baris masih 0 dan mengarahkan operator ke tombol Batalkan (FE-only, nol sentuhan DB, **tidak** melonggarkan guard). Itu menutup **jalur lahirnya** sub-kasus terkeras untuk data baru. **Yang MASIH jadi keputusan terbuka:** opsi **"longgarkan `cancel_picking` untuk picking `done` yang belum punya SJ ter-dispatch"** sengaja **DI-SKIP untuk sekarang dan belum gugur** — hanya opsi itu yang bisa menolong baris `done` nol-pick yang **sudah terlanjur ada di DB** (hari ini: intervensi SQL manual), dan kasus umum #31 tak tersentuh sama sekali. Opsi "batalkan sisa" belum dipilih; opsi "biarkan" tidak dipilih. ⚠️ Rujuk opsi lewat **namanya**, bukan huruf — label (a)/(b) sempat bertabrakan antar-sesi (lihat peringatan di baris TD-206).
32. **[5 Sep 2026 · ✅ DIPUTUSKAN 6 Sep 2026 — TETAP SCHEMA-ONLY]** **`schema_snapshot.sql` — data ikut di-dump atau tidak?** **JAWABAN: TIDAK.** Perintah baku kini `pg_dump --schema-only --schema=public`; `--no-owner`/`--no-privileges` tetap DILARANG (ACL harus ikut). `02_RULES_GOVERNANCE.md` §4 sudah diselaraskan — verifikasi `COPY ~133` dicabut, diganti `CREATE TABLE public.` = 133 + `GRANT … TO authenticated` tidak boleh turun. Alasan lengkap + jejak auditnya ada di §4 itu; ringkasnya: snapshot lahir schema-only dan bertahan 119 commit, data ikut 31 Agu tanpa keputusan tertulis, aturan yang mewajibkannya ditulis 2 Sep (dua hari sesudahnya) dengan alasan yang hanya menyangkut ACL, nol dari 680 rujukan dokumentasi memakai datanya, sementara versi berdata membawa 385 email + data pelanggan permanen ke riwayat Git (9,62 MB vs 704 KB). **Arsip pertanyaan aslinya di bawah:** Sejak 5 Sep 2026 perintah refresh berganti jadi `pg_dump --schema-only --schema=public`. Penambahan `--schema=public` **jelas perbaikan** (dump lama ikut membawa 35 tabel skema sistem `auth`/`storage`/`realtime`/`vault`; jumlah tabel `public` tetap **133 ↔ 133**, nol yang hilang). Yang **belum diputuskan** adalah `--schema-only`: ia menghapus seluruh **133 blok `COPY`** dari file, sementara `02_RULES_GOVERNANCE.md` §4 — ditulis 2 Sep 2026 — **melarang flag itu secara eksplisit** dan mewajibkan verifikasi *"kalau `COPY` jatuh ke 0 → JANGAN commit"*. Argumen kedua sisi sama-sama nyata: alasan doc 2 Sep adalah **auditabilitas ACL/data dari repo**, sedangkan schema-only berarti **repo tak lagi membawa data produksi** (relevan walau repo sudah private). ⚠️ Yang **tidak** jadi taruhan: ACL selamat — `GRANT` tabel `public` ke `anon` identik **105 ↔ 105**, jadi TD-24 tetap bisa diaudit dari repo. **Perlu keputusan Den**, lalu `02_RULES_GOVERNANCE.md` §4 diselaraskan mengikuti keputusan itu (dokumen dan praktik sekarang saling bertentangan — jangan dibiarkan). Detail teknis: `03_DATA_MODEL.md` **gotcha #23**.
33. **[5 Sep 2026] ~65% nilai kontrak nyangkut di tahap PENERBITAN INVOICE — bottleneck-nya di sana, bukan di pembayaran customer. Perlu dikonfirmasi ke Elvira SEBELUM dibawa ke Pak Adam.** Angka produksi 5 Sep 2026: **Outstanding Tagih = 422 SP / Rp 6.273.578.117** — barang **sudah dikirim** dan **BTB sudah terbit**, tapi **belum satu pun diinvoice**. Bandingkan: piutang yang sudah ditagih hanya **Rp 47,3 jt**, sedangkan nilai kontrak seluruh SP **Rp 9,61 M**. ⚠️ **Ini temuan bisnis, bukan temuan kode** — sistemnya bekerja sesuai desain; yang belum diketahui adalah **kenapa** invoice tak terbit (backlog kerja Finance? menunggu dokumen customer? proses di luar sistem? atau justru sebagian sudah ditagih di luar Nexus sehingga angkanya menyesatkan?). **Jangan bawa angka ini ke manajemen sebelum Elvira mengonfirmasi bacaannya.** Catatan yang harus ikut disampaikan: angka ini bersumber dari `sp_items` (gotcha #21) dan syarat "invoice hidup"-nya `status <> 'void'` tanpa `deleted_at` — sengaja identik dgn guard `create_invoice`.
34. **[5 Sep 2026] Siapa audiens Laporan Per Barang, dan lewat akun apa dia membukanya?** Dua fakta yang bertabrakan: **(a)** akun **Azhar Malik** — satu-satunya pemegang role `ceo` — **`is_active = false`**, jadi audiens utama laporan berorientasi CEO ini **belum punya akun hidup**; **(b)** gate export/print bersandar pada menu key **`logistics_sp`** (`MENU_KEY_MAP` `App.jsx:1278` — Dashboard Storbit sengaja memakai ulang key ini), dan aksesnya hari ini dipegang **super_admin + Elvira Nurhuda + Gigih Rizky Cahaya** saja, seluruhnya lewat grant **user-level** (`role_menu_permissions` untuk `logistics_sp` = **0 baris**). Perlu diputuskan: aktifkan akun CEO? tambah baris `role_menu_permissions` untuk role `ceo` (dan siapa lagi)? atau laporan memang dimaksudkan **dicetak oleh Finance lalu diserahkan**, bukan dibuka sendiri? ⚠️ **Fakta (a) dan (b) adalah klaim Den yang di-relay** — doc-keeper nol akses DB, tidak memverifikasinya sendiri; yang diverifikasi doc-keeper hanya pemetaan menu key di `App.jsx:1278`. Kerabat: **Fase B3** seeding `role_menu_permissions` yang memang belum dimulai.

> ⏭️ **#35 SENGAJA DIKOSONGKAN di `main`.** Nomor itu milik branch `feature/crm-v3-batch-persiapan` (blokir RENAME `crm_v3_lifecycle`, jalur A vs B) hasil renomori 6 Sep 2026. Jangan dipakai untuk hal lain — memakainya akan mengulang tabrakan penomoran yang baru saja dibereskan.

36. **[4 Sep 2026 · ✅ TERJAWAB 6 Sep 2026] `set_prospect_on_inquiry` dipersempit — benar-benar disetujui, atau terselip?** **JAWABAN: isinya DISETUJUI, tapi eksekusinya DIPISAH dari migrasi lifecycle.** Mengeluarkan `sql` dari daftar tahap yang dinaikkan ke `prospect` — dari `('lead','mql','sql')` jadi `('lead','mql')` — **benar secara isi**, dengan tiga alasan: **(1)** lifecycle harus **monoton naik** (prinsip blueprint CRM v3), sedangkan `sql` berada **DI ATAS** `prospect` sehingga perilaku sekarang **menurunkan** akun satu tingkat — melanggar prinsipnya sendiri; **(2)** status `sql` **mahal didapat** (butuh penilaian BANT), dan membuangnya karena satu inquiry masuk **menghapus hasil kualifikasi**; **(3)** **sinyalnya terbalik** — membuat inquiry itu kabar baik, tapi perilaku sekarang justru menghukumnya. ⛔ **TAPI perubahan perilaku bisnis TIDAK BOLEH menumpang di migrasi yang judulnya rename kolom.** Kalau hari ini lolos karena isinya kebetulan benar, besok yang isinya **salah** lolos dengan cara yang persis sama. Itu soal jalur, bukan soal isi.
    **BENTUK AKHIR — DUA migrasi terpisah:**
    - **(a) Migrasi lifecycle jalur B** (prasyarat merge). Fungsi `set_prospect_on_inquiry` **DISALIN APA ADANYA dari produksi**, daftar tahapnya **TETAP `('lead','mql','sql')`**, **NOL perubahan perilaku**. ⛔ **INI MENGIKAT:** siapa pun yang menulis migrasi itu **JANGAN membawa penyempitannya** — **walaupun header migrasi lama (`20260827000002_crm_v3_lifecycle`) mengklaim sudah disetujui Den.** Klaim itu bukan izin untuk menggabungkannya.
    - **(b) Migrasi perilaku BERDIRI SENDIRI.** Dijalankan **setelah branch `feature/crm-v3-batch-persiapan` di-merge ke `main` DAN berjalan stabil di produksi** — **bukan di hari H merge**. Judul dan deskripsinya **wajib menyebut eksplisit angka yang bergeser**: jumlah akun ber-`lifecycle` **`sql` NAIK**, **`prospect` TURUN**, dan efeknya **hanya untuk inquiry BARU**.
    **⚠️ UTANG YANG MENEMPEL PADA (b) — pertanyaan terbuka, sengaja BUKAN TD lepas:** migrasi (b) menghentikan kejadian baru tapi **TIDAK memperbaiki akun yang sudah terlanjur turun** ke `prospect`. **Sebelum (b) dijalankan: hitung dulu berapa banyak**, lalu **Den** memutuskan dikoreksi atau dibiarkan. Ini **keputusan bisnis**, bukan teknis — mengoreksinya berarti **mengubah angka historis** (funnel, konversi MQL→SQL, laporan yang sudah beredar). Menaruhnya sebagai TD lepas akan membuatnya tampak seperti pekerjaan rapi-rapi yang bisa diambil siapa saja; ia harus tetap menempel pada (b).
    **⚠️ Branch belum selaras.** `feature/crm-v3-batch-persiapan` masih menyimpan pertanyaan yang SAMA sebagai **Keputusan Terbuka #36 dalam keadaan TERBUKA** (hasil renomori 6 Sep; sebelumnya #33). Branch **sengaja tidak disentuh** saat keputusan ini dicatat. **Saat sinkron: butir #36 versi branch harus diganti dengan versi TERJAWAB ini** — nomornya sama, jadi keduanya melebur jadi satu butir.
